# Nice Ink — UI 系統規範（UI System）

> **這份文件的唯一理由：畫面沒有人擁有。**
> 2026-09-02 的事故（`LINER` 同時出現在三個地方）已經由
> `Docs/DRAW_HUD_INVENTORY.md` 在**作畫相位**內部解決；但那份清單只管一個相位。
> 全遊戲有 **9 個相位 ＋ 7 個選單頁 ＋ 3 種特殊視野**（沉睡／裝睡／開場動畫），
> 而其中只有一個相位有人在管。這份文件管的是**跨相位的那一層**。
>
> 分工：
> - **SPEC.md** ＝設計的唯一權威（玩法、機制、承重不變量）。本文件不做設計決定。
> - **本文件** ＝畫面的憲法：分區職責、載體選擇、操作語言的形式、每個相位要說哪一句話。
> - **DRAW_HUD_INVENTORY.md** ＝作畫相位的逐元素清單（本文件的第一個實例）。
> - **NiceInkUiTokens.h** ＝數值正本（字級／間距／顏色）。**本文件不寫數值，只寫規則。**
>   註解裡的常數會過期而沒有東西看著它（專案鐵律）。

---

## 一、Meccha Chameleon 實證與抄襲判決（2026-09-03，**二修**）

> **本節在同一天被推翻過一次，過程本身是這份文件最重要的內容，不要刪。**
> 一修我只看了十張局內截圖，並據此宣告「他們的工具 UI 沒有東西可以抄」。
> user 反問「你是眼睛沾到狗屎所以只能看到色票器嗎」——**而那張截圖的左緣就是
> 他們完整的操作圖例，我把整個畫面的判決建立在畫面中間那一塊上。**
> 教訓寫在 §1.5。

**證據等級**：
- **A**：Steam 官方截圖 10 張（日文）＋社群截圖（**英文版**，證明字串是本地化的）
- **A**：官方預告片逐 2 秒抽格 12 張（`ffmpeg` → contact sheet）＝**局內未出現在截圖裡的畫面**
- **A**：安裝目錄 `Chameleon-Windows.utoc` 資產索引（~140 個 `WBP_*`、字型、輸入圖示資料表）
- **C**：開發者訪談（兩人／兩個月）——只用來理解取捨壓力

### 1.1 他們的「操作提示」不是鍵帽＋動詞，是一套**輸入片語文法**

一修我寫成「鍵帽＋2~5 字動詞」。**實物有三個成分，而且可以組合**：

    [動作圖示]  [輸入 glyph 序列]  動詞
                 ↑ 可以用 + 串接、可以上下堆疊成替代綁定

實例（paint mode 左緣圖例，預告片第 6 格）：

| 圖示 | 輸入 | 動詞 |
|---|---|---|
| 滴管 | `[SPACE]` | `3Dスポイト(長押し)` ← **長按這件事寫在動詞裡** |
| 左右箭頭＋筆 | `[滾輪 glyph] ＋ [CTRL]` | `ブラシサイズ` |
| 光芒 | `[V]` | `影` |
| — | `[右鍵 glyph]＋[移動 glyph]` **／** `[ALT]＋[左鍵]＋[移動]` | `カメラ回転` ← **兩組替代綁定上下堆疊，共用一個動詞** |
| — | `[滾輪 glyph]` **／** `[ALT]＋[右鍵]＋[移動]` | `ズーム` |

**滑鼠不寫 `RMB`——畫一隻滑鼠，把那顆按鈕點紅。** 這是「鍵位要畫成鍵盤上的樣子」
這條規則的完整版：**滑鼠沒有刻字，所以它的「字形」就是那隻滑鼠的圖**。

**鍵帽底色帶狀態**：截圖 1 底部 `[CTRL 青] しゃがむ`／`[CTRL][SPACE] 綠 立ち上がる`
——組合鍵**包在同一個深色圓角容器裡**＝一個動作。截圖 1 右緣 `[2 綠][3 綠]` 是切換開關，
截圖 2 的 `[1 黃] 挑発` 是一次性動作 ⇒ **顏色在區分「切換／動作／當前狀態」，不是重要性**。

### 1.2 他們**有**一套教規則的東西，而且是常駐的

右下角永遠掛著：**綠色模式名 ＋ 兩行規則**。英文版實物：

    Infection
    If you get caught, you become a hunter.
    Hide until the end to win

日文版是 `増え鬼` ＋兩行。**這是「怎麼玩」的答案，不是「按哪顆鍵」的答案**——
兩者是不同的東西，他們用不同的區塊、不同的顏色、不同的字級各講一次。
一修我看到 `増え鬼` 卻把它當成裝飾性的模式名跳過去了。

### 1.3 其餘實測（一修已對的部分，保留）

- 上緣中央＝沙漏＋大數字＋**祈使句**（`隠れつづけよう`／`探索開始まで`／`Search Time`）
- 上緣＝生存者／獵人火柴人 icon 排；右下角＝`残り人数` 巨大數字（本局唯一比分）
- 右緣縱列＝模式類（`Paint Mode`／`Rotation Lock`／`Toggle Nameplate Display`／
  `Toggle X-Ray Rendering`），**每列右側還有一個圓形線稿象形圖**＝動作本身的圖解
- 底部中央＝姿勢類（`Crouch`／`Stand Up`／`Climb`）
- **動詞是本地化的**（英文版是 2~4 個單字，日文是 2~7 字）⇒ **版面必須由量測寬度驅動，
  不能寫死欄寬**
- 字型 `MPLUSRounded1c` ＋ `ZenKakuGothicNew` ＋ Noto SC/TC/KR/Arabic fallback 矩陣
  ——與我們 08-06 的六文字系統矩陣同一種做法
- **沒有任何 tutorial 資產**（`Tutorial|Guide|Hint|Onboard` 全索引只命中 `BGM_Tutorial`）
  ⇒ **教學＝常駐圖例 ＋ 上緣祈使句 ＋ 右下規則塊，沒有第四種東西**

### 1.4 判決（二修）

| 項目 | 判決 | 依據 |
|---|---|---|
| **輸入片語文法**（動作圖示＋glyph 序列＋動詞，可串接可堆疊替代） | **抄** | 我們現在只有「鍵帽＋動詞」＝這套文法的最簡子集，無法表達長按、組合鍵、替代綁定 |
| **滑鼠畫成滑鼠、亮起那顆鍵** | **抄** | 我們現在寫 `LMB`／`RMB` 純文字 |
| 鍵帽底色＝切換／動作／狀態 | **抄** | 我們現有 `bAccent` 語義是「重要」——一個通道兩個語義 |
| **右下角常駐規則塊**（模式名＋兩行怎麼贏） | **抄** | 我們**完全沒有**這個東西。玩家目前無處得知「猜錯會怎樣」 |
| 上緣「倒數＋祈使句」同一群組 | **抄** | 我們上緣是名詞（`GALLERY TOUR`） |
| 鍵帽資料驅動（一張表＋輸入裝置） | **抄** | 見 §4.3 |
| 中性 chrome（近白底＋深字鍵帽） | **可抄，且實測我們已經是** | 他們 `248,248,248`；我們 `Paper` ＝ L*92 **a*0** b*+10 ⇒ ΔE 11.3，紅綠軸零偏移。差異只在強調色色相（他們綠／我們酒金，ΔE 99） |
| UE 編輯器色票器本體（RGBA 滑桿／Hex／メタリック／ラフネス） | **不抄** | 這一塊確實是引擎預設值原封不動搬上螢幕（連 `Overwrite color picker on sample` 都沒翻譯）。**但這是那個畫面裡唯一沒設計的一塊，不是那個畫面的全部** |
| 房間／配對（伺服器瀏覽器） | **不抄** | 08-05 已定案（他們的 join UX 被自己玩家證明是坑） |

### 1.5 方法論記帳（**這條比上面整張表值錢**）

- **我用畫面中央的一塊，判決了整個畫面。** 色票器佔那張截圖約 12% 的面積，
  左緣圖例、右緣模式列、右下規則塊全都在同一張圖上，我一個都沒讀。
  **「這裡沒有設計」這種全稱判決，要先證明你看過全部。**
- **十張截圖不是「他們的 UI」，是「他們願意放上商店頁的十個瞬間」。** 補救成本極低：
  預告片抽格（`ffmpeg` 一分鐘）＋社群截圖（英文版）就翻出三個新元件。
  **樣本來源單一＝結論的信賴區間比你以為的寬得多。**
- 同族血價：`project-ink-canvas-quality` 的「兩個症狀互斥＝你在錯的層」、
  `feedback-verification-discipline` 的「先證明我量的那條線＝他看的那條線」。

## 二、兩個載體，一套語言

| 載體 | 用在哪 | 為什麼是它 |
|---|---|---|
| **Slate C++**（`SNiMenu`） | 主選單七頁（Root/Host/Join/Settings/Credits/Language/Profile） | 需要毛玻璃半透明白卡、文字輸入、清單捲動——canvas 做不到（08-06 user 裁決） |
| **Canvas**（`ANiceInkHUD`） | 局內全部（含大廳、ESC 選單、沉睡黑屏） | 每幀跟著世界走、零資產、與 3D 同一條繪製時序 |

**兩者共用**：`NiHudColor`（顏色）／`NiType`（字級角色表）／`BuildCompositeUiFont`
（六文字系統矩陣）／FlowDirection 鏡像規則。**樣式只有一套，載體有兩個。**

**選哪個的判準**：需要**打字或捲動** ⇒ Slate；其餘一律 canvas。
（新增第三個載體＝新增第三套樣式，永遠不要。）

---

## 三、局內畫面分區（五區＋零度區）

以視線為原點分配，不是以版面美感分配。

| 區 | 職責 | 允許的內容 | 禁止 |
|---|---|---|---|
| **0°：螢幕中心** | 落筆當下會被讀到 | 只有「墨會落在哪、多粗、什麼顏色」 | 任何文字、任何狀態、任何教學 |
| **上緣中央** | 這個相位是什麼、還剩多久、**現在該做什麼** | 相位祈使句＋倒數＋對象（受害者臉／進度） | 操作提示 |
| **右緣縱列** | **操作語言的唯一位置** | 鍵帽＋動詞，隨狀態增減 | 狀態值、數字、句子 |
| **下緣中央** | **活的狀態與事件**（一次至多一行） | 約束（搆不到）、表決、當前墨杯 chip | 常駐操作表（已退役，不要加回來） |
| **右上／右下角** | 資源與比分 | 現金、罰酒杯數、剩餘人數類 | 其他一切 |
| **右下角（規則塊）** | **這個相位怎麼玩、怎麼贏**——與「按哪顆鍵」是兩件事 | 相位名（強調色）＋至多兩行規則 | 操作、狀態、數值 |

**淡出規則**：按住 LMB（落筆中）＝上緣 chrome 淡到 35%、右緣操作列淡到 30%。
理由：那幾秒它們只是視野邊緣的亮塊。

---

## 四、操作語言（唯一形式）

### 4.1 形式＝輸入片語（**09-03 二修：從「鍵帽＋動詞」升級**）

    [動作圖示?]  [glyph 序列]  動詞
                  ↑ `A ＋ B` 串接；替代綁定上下堆疊共用一個動詞

- **glyph**：鍵盤鍵＝淺底圓角鍵帽＋深色鍵名（`DrawKeycap`，已有）；
  **滑鼠＝畫一隻滑鼠、把那顆按鈕點紅**（待做）；滾輪＝滑鼠＋上下箭頭；
  移動＝滑鼠＋四向箭頭。**鍵盤有刻字所以寫字，滑鼠沒有刻字所以畫圖。**
- **長按／點按的差別寫進動詞**（`3Dスポイト(長押し)` 的作法），不要另造一個視覺通道。
- 動詞＝**動作**不是名詞（`沾墨` 不是 `墨杯`；`指認` 不是 `嫌疑人`）。
- 鍵名**不翻譯**（鍵盤刻字）；動詞**必須進字串表**。
  **動詞長度隨語言變（英文 2~4 字、日文 2~7 字）⇒ 版面一律量測寬度驅動，禁止寫死欄寬。**
- **動作圖示是選配**：只在「動詞說不清楚這個動作長什麼樣」時加（筆寬、相機旋轉）。
  我們的三支筆在針尖的視覺本來就不同，不需要圖示（見 `DRAW_HUD_INVENTORY` 已刪清單）。

### 4.2 何時列、何時不列（鐵則）
- **軸不存在的時候，它的輸入、它的格子、它的操作表那一行都不該存在。**
  留著一顆按了不會有事的鍵，玩家會以為是自己弄錯了。
  （實例：`SCROLL 灰洗` 只在打霧筆列出，因為濃度只有打霧吃得到。）
- **一件事只講一次**，講在會被讀到的地方。
- **要背 vs 不用背**才是砍通道的判準：任意映射（數字↔顏色）砍掉，
  自然映射（滾輪↔濃度：有序手勢配有序軸）留著。

### 4.3 資料驅動（**待做，本文件的第一號工程**）
現況：每個呼叫點手寫 `Rows.Add({ TEXT("Q"), ENiLocKey::ActNeedle, false })`。
目標：一張 `FNiControlHint { EKey, ENiLocKey, EAvail }` 表，由相位／狀態查詢，
鍵形由**輸入裝置**決定（Meccha 的 `T_InputDeviceDatas` 對應物）。
**沒有這一層，手把支援或改鍵功能一到就要全站重寫。**

### 4.4 鍵帽底色的語義（**收斂**）
現況 `bAccent` 語義是「這條最重要」。**改成「現在可按 / 現在不可按」**
（Meccha 的作法），重要性交給排序（第一列＝這個狀態下唯一要學的動作）。
一個視覺通道只能有一個語義。

---

## 五、相位祈使句表（**每個相位一句「現在該做什麼」**）

Meccha 教學的另外一半。我們現在上緣只有**名詞**（`GALLERY TOUR`），
而名詞說明不了要做什麼。

**現況（2026-09-03 實測）**：9 個相位中只有 `Drawing` 進了字串表，其餘 8 個
是硬編英文（`GetPhaseLabel`）。

| 相位 | 現況上緣 | 該有的祈使句（**草案，待 user 文案定案**） | 該有的操作列 |
|---|---|---|---|
| Lobby | `LOBBY` | 等大家坐滿 | `ENTER 開局`（房主）／`ESC 踢人`（房主） |
| BottleSpin | `BOTTLE SPIN` | 看瓶口指向誰 | 無（純儀式） |
| Seating | `SEATING` | 他要睡了 | 無 |
| Drawing（站著） | ✅ 已在表 | 湊近他的身體 | `RMB 湊近`／`F 翻身`／`G 搖夢` |
| Drawing（鎖定） | ✅ 已在表 | 畫下去 | `LMB 上墨`／`RMB 沾墨`／`SCROLL 灰洗`／`Q 換筆`／`G 搖夢`／`WASD 起身` |
| 沉睡（描圖） | `DRUNK DREAM` | 沿著線描到底 | `按住 LMB 描線` ＋進度% |
| 裝睡 | 只有一行提示 | 裝睡中，隨時可以睜眼 | `SHIFT 放開＝睜眼` |
| Tour | `GALLERY TOUR` | 看清楚每一幅 | 無（同步演出） |
| Accusation（受害者） | `ACCUSATION` | 指出這幅是誰畫的 | `1-9 選畫`／`TAB 換人`／`ENTER 指認` |
| Accusation（旁觀） | `ACCUSATION` | 別讓他認出你 | 無 |
| Resolution | `RESOLUTION` | （結果演出自己會說話） | 無 |
| Finale | `FINALE` | — | 無 |
| PostGame | `PARLOR` | 端詳你的刺青 | `L 雷射`／`ENTER 下一場`（房主） |

**鐵則**：祈使句是**給誰看的**要先答得出來（受害者與作畫者在同一相位看到的句子不同，
上表已分行）。答不出來的相位就不要有句子。

---

## 六、本地化（畫面上每一個字）

- **正本＝`NiceInkLocText`（13 語）。畫面上任何會被讀的字都必須是一個鍵。**
- 例外只有兩類，各自要有理由：①鍵名字形（鍵盤刻字）②純版型格式（`%d / %d`）。
- **閘門＝`Tools/UiCheck/hud_loc_lint.py`**。現況 `GUARDED` 只含作畫相位九個函式
  ——**這是誠實的邊界標記，不是完成度**。
  目標：`GUARDED` 擴到 `NiceInkHUD.cpp` 全檔（＝這份規範的完成定義之一）。
- 血價：「我以為我刪掉了」要用工具查不要用記憶查——lint 第一次跑就抓到我漏的 5 條。

---

## 七、閘門（離線、不開引擎）

| 閘門 | 守什麼 |
|---|---|
| `Tools/UiCheck/tray_layout_check.py` | 墨杯盤版面／六解析度不出界／棋盤對比工作區間 |
| `Tools/UiCheck/tray_cursor_check.py` | 托盤游標增益：FOV 無關、跟 UI 縮放 |
| `Tools/UiCheck/hud_loc_lint.py` | 裸英文字面量（範圍見 §6） |
| **待做** | 相位覆蓋閘門：每個相位×每個角色，操作列與祈使句是否存在（**空白就是缺陷**） |

**閘門鐵則（兩條血價）**：
1. **閘門必須去讀真相的正本**——把結論抄一份到自己身上再核對，等於沒有閘門。
2. **寫閘門時要問：什麼樣的壞會讓它照樣亮綠燈？**（密度閘門量面積、失敗是各向異性
   ⇒ 它不是失效，是從一開始就沒有解析度。）

---

## 八、記帳（刻意未做／待裁）

- **右下角規則塊**：我們完全沒有。玩家目前無處得知「猜錯會怎樣／第三杯會怎樣」——
  這是 Meccha 教規則的整個機制，而我們的規則比他們複雜得多。**文案待 user 定案。**
- **滑鼠 glyph**：現在寫 `LMB`／`RMB` 純文字；要畫成滑鼠圖需要一張小貼圖或程序化畫法。
- **手把支援**：目前零支援。§4.3 的資料驅動層是它的前置；**要不要支援是 user 的決定**，
  但**現在不做那一層，將來就是全站重寫**。
- **改鍵**：同上。
- **相位祈使句的文案**：§5 是草案，逐句待 user 定案（文案＝口味域）。
- **完全中性化（待 user 裁決）**：局內 chrome 由 `Paper/Ink` 改純白／純黑＝三個常數、零風險；
  **但強調色酒金目前同時承載「現金／警告／重要鍵帽」三個語義**，拿掉顏色通道就要改用位置或粗細分開。
  且 `Paper` 同時是選單毛玻璃白卡的底（08-06 user 驗收過）——動它會一起動到選單。
- **Slate 選單頁的鍵帽語言**：選單目前是純滑鼠，無鍵帽。手把支援若上，這裡要一起。

---

## 八之二、glyph 資產與第一輪離線對照（2026-09-04）

**資產＝Kenney Input Prompts 1.5（CC0）**，正本存 `SourceAssets/InputPrompts/`
（含 `LICENSE_kenney.txt`）。1,280+ 個 PNG，**白色剪影 on transparent**
⇒ 與我們 `DrawIconTok` 的 tint 管線同構（`IconSpray`／`IconKick` 已是這個模式）。
CC0＝可商用、免署名（仍會在 `THIRD_PARTY_NOTICES` 記一行，這是禮貌不是義務）。
滑鼠圖自帶紅色高亮鍵 ⇒ 重染成我們的 `Amber`。

**離線對照工具**：`Tools/UiCheck/mkstrip.py`／`mkstrip2.py`
（把 glyph 合成到**真實作畫截圖** `directdraw_fp_paint.png` 上，不開引擎、一輪 20 秒）。
成品＝`Saved/UiMock/glyph_style_*.png`。

**三輪迭代推翻了我自己的兩個提案**：

1. **「整排換成圖示」是錯的。** 13px 下 Kenney 的字母鍵（字是挖空的）與線稿版全部糊掉。
   **回頭看 Meccha 實物：他們的鍵盤鍵也是「白鍵帽＋文字」，只有滑鼠才畫圖。**
   ⇒ 正解＝**鍵盤維持現況、只有滑鼠換成圖**（滑鼠沒有刻字，所以它的字形就是那隻滑鼠）。
2. **深色底板是錯的。** 對比從 3.13 拉到 8.73，但每列一塊底板＝鋸齒狀色塊，
   存在感遠超過 chrome 該有的。Meccha 也沒用底板——**他們用的是帶陰影的白字。**

**同時量到一個現況的 bug（與 glyph 無關）**：右緣操作列的動詞用 `PaperDim`，
在**膚色背景**（作畫時畫面主體就是皮膚，`163,121,110`）上對比 **1.41**
——任何標準都不及格，等於那五行動詞現在是隱形的。改 `Paper` ＝ 3.13。
**這條要修，跟選哪個 glyph 風格無關。**

### 八之二之二、mock 本身的品管（**2026-09-04 血價，user：「死人排版嗎」**）

第一版 mock 我自己排壞了三處，而且把它拿給 user 挑：
①鍵帽字沒置中（y 寫死成 `1`、拿 `getbbox()[2]` 當前進寬度）②用 9px 畫完再放大 3 倍
（看到的字距崩壞是放大點陣的產物）③用 Segoe UI 而不是我們的字。
**把我的 render bug 混進 user 要判斷的差異裡，等於請他評審一張雜訊。**

**要澄清的重要一點**：遊戲裡的 `DrawKeycap` **本來就是置中的**
（`EHAlign::Center` ＋ `(H-Size.Y)*0.5`）——歪的是我的 mock。**不要去「修」一個沒壞的東西。**

現制＝`Tools/UiCheck/strip_mock.py`，三條規則寫進工具本身：
1. **字體＝遊戲同一支**：從 cooked `.ufont` 抽 TTF（4 byte 長度前綴後接 `00010000`），
   動詞 M+ Rounded Medium／鍵名 XBold（對應 C++ 的 `bBold`）。**mock 不准用替代字體。**
2. **幾何＝1080p 原生**（UiScale=1），要放大是放大 1080p 的成品。
3. **置中閘門**：量渲染後墨跡 bbox 的偏心，>1.0px 即 assert（現況 PASS）。

**可攜鐵則：交給 user 判斷的圖，它自己必須先通過品管**——否則你問的是
「你喜歡 A 還是 B」，他看到的卻是「你為什麼把字排成這樣」。

**同批修掉**：滑鼠 glyph 亮起的那顆鍵原本沿用 Kenney 的紅 `231,50,70`（不在我們的
調色盤裡），改成 `NiHudColor::Red 217,79,61`，且**兩隻滑鼠一致**——
「哪顆鍵」是恆定語義，重要性交給**列的順序**（§4.4 已定：底色語義收斂成可按／不可按）。

### 八之二之三、版面一致性的六條（**2026-09-04 二次血價，user：「你可以自己量一下再給我嗎」**）

第一版 mock 通過了「置中」與「用對字體」，我就交了——**而版面本身四軸全破**。實測：

| 軸 | 我交出去那張 | 修正後 |
|---|---|---|
| 行距（相鄰列中心距） | **32 / 29 / 26 / 26**＝三種 | 26 恆定 |
| 動詞右緣 | **極差 67px**（沒有對齊軸） | 極差 **0** |
| glyph 高度 | **27 / 27 / 19 / 19 / 19** | 全 19（或全 24.7） |
| 間距常數 | 8✓ 12✓ **2✗ 5✗** | 4 / 8 / 4 全在網格 |

真因：**我讓每一列各自算自己的版面**（`y += max(ROW_H, h+5)`、動詞位置從各列的
glyph 左緣往左推）⇒ 行距與文字右緣都變成「內容的函數」。
**一致性必須由版面模型保證，不能由每列各自算出來。**
現制＝`build_rows()` 先算完 `maxgw`／`pitch`／`verb_right` 再畫。

**這類 chrome 最基本要一致的六件事**（寫下來當檢查表）：

1. **單一對齊軸**——一組元素只能有一條共用邊；兩條散開的邊＝沒有版面。
2. **固定行距**——垂直節奏是讀「這是一組」的唯一線索。
3. **同層元素同尺寸**，但判準是**視覺等重不是幾何等高**（圖形在同高度下比字母輕）。
4. **所有間距出自同一個尺度**——不准出現 2、5 這種隨手數字。
5. **同一語義同一顏色**——兩隻滑鼠的高亮鍵用兩種紅＝在說兩件事。
6. **一個視覺通道只承載一個語義**（§4.4 的 `bAccent` 收斂同理）。

**閘門已寫進 `Tools/UiCheck/strip_mock.py`**（置中 ＋ 間距落在 4px 網格）
與 `strip_measure.py`（行距／對齊軸／glyph 等高的逐項對賬）。

**已記帳未動**：出貨的 `CAP_H=19`／`ROW_H=26` 本身**不在 4px 網格上**。
`NiSpace` 的網格註解自己寫著「選單 Slate 版面用」＝局內 HUD 從未宣稱在這條網格上。
**要不要讓局內 HUD 併入同一個網格（20／24）是 user 的決定——那會動到已驗收的畫面，
我不自己改。**

**待 user 指定**：`Saved/UiMock/glyph_style_final.png` 的 A／F1／F2 三選一。
最終仍以 viewport 為準（鐵律一）。

---

## 八之三、全站現況稽核（2026-09-04，`Tools/UiCheck/ui_token_audit.py`）

> **這一節才是 user 原本的題目。** 我從 09-04 第二輪起鑽進「操作提示列」一個 widget
> 迭代了六版，而他要的是**整個遊戲的 UI**。記在這裡當警戒線：
> **把一個症狀修到完美，不等於處理了他交代的範圍。**

**受測面**：選單 7 頁（Root／Host／Join／Settings／Credits／Language／Profile）
＋局內 17 種畫面狀態（9 相位 × 角色分歧 ＋ 沉睡描圖／裝睡／開場動畫／ESC 選單／
進房載入布／搖夢回執／翻身表決／兇手轉盤）＝**24 個面**。

### 五項可量的失敗（掃原始碼，不是看截圖）

| # | 項目 | 實測 | 判定 |
|---|---|---|---|
| ① | **圓角半徑** | 全站 **9 種**：1／1.5／2／4.5／10／12／14／16／18 | FAIL 沒有階梯 |
| ② | **面板** | 局內＝`Ink` 墨底、**alpha 7 種**（.55/.60/.62/.72/.80/.85/.88）無規則；選單＝`Paper` 紙底 α.78＋blur14 | 見下（部分平反） |
| ③ | **按鈕樣式** | 5 種；`GhostStyle` 同時穿在**導覽**（Profile／Settings／Back）與**離開遊戲**（Quit）身上 | FAIL 同視覺不同後果 |
| ④ | **明朝體＝品牌聲部** | 選單 10 個字級角色中 5 個是明朝體（Display／Title／Value／Action／ActionSmall）；**局內四個 tier 連 serif 旗標都沒有** | FAIL 一款遊戲兩種字體人格 |
| ⑤ | **間距網格** | `NiSpace` 4/8/16/24（註解自陳「選單用」）；局內 CAP_H 19、ROW_H 26 → 19%4=3、26%4=2 | FAIL 局內沒有網格 |

**② 的部分平反（想清楚才寫）**：局內背景是**明亮的皮膚**、選單背景是**黑**
⇒ 墨底與紙底各自都是對的，**面板顏色本來就該取背景的反相**。
所以 ② 不是「顏色不一致」的缺陷，是**沒有把規則寫下來**＋**alpha 七種沒有規則**。
（差點把一個正確的決定判成 bug——`SPEC_RECONCILE` 的分類鐵則同款：
**實作對而規範沒寫 ⇒ 補規範，不是改實作。**）

### 待 user 裁決（每條我都給推薦，可以直接否決）

1. **半徑收斂成三階＋線**：卡 18／控制項 12／小件 4.5／線 1。**推薦做**（純樣式、零玩法風險）。
2. **面板 alpha 收斂成三階**：抬頭 .55／內容 .72／模態 .88，並把「面板取背景反相」寫成明文。**推薦做**。
3. **明朝體進局內**：相位標題（`DRAWING`／`GALLERY TOUR`）改明朝體＝品牌聲部；
   操作提示與數值維持圓體。**推薦做**——這是四項裡對「質感」感受最大、風險最低的一刀。
4. **局內併入 4px 網格**（CAP_H 19→20、ROW_H 26→24）。**推薦做，但會動到你已驗收的畫面**，所以要你點頭。
5. **Quit 與導覽分色**（Quit 給獨立的低調紅或移出那一排）。**推薦做**。

### 這一節的方法論記帳

- 稽核工具第一版**自己有 bug**：抓半徑的正則在 `MakeRounded(WithA(X, 0.78f), 18.0f)`
  的內層逗號就停了 ⇒ 把 alpha 當成半徑，報出 13 種。**閘門本身要被測**（專案既有鐵則，
  這是第三次踩）。修法＝取最後一個引數。
- **稽核要掃原始碼不要看截圖**：截圖只證明「那一幀長這樣」，掃碼才知道「有幾種值」。

---

## 八之四、施工中：Meccha 設計語言落地（2026-09-04 起）

**user 定案**：「照抄 Meccha Chameleon 的設計語言」＋「中性 UI」。
＝**參照已經給了，不要再問**（我問了兩次，違反鐵律二）。

### 尺度正本（`NiUi`，`NiceInkUiTokens.h`）

**基準 U = 4px @1080p，所有尺寸與間距都是 U 的整數倍。**
三個關鍵值**不是挑的，是量 Meccha 實物**（`meccha_2.jpg` 1920×1080 右緣、暗磚背景、
白色連通帶）：

| 量到的 | 值 | 我們採用 |
|---|---|---|
| 元件高（glyph＋動詞） | 47~48px | `48 = 12U`（KeycapH 32 ＋ GapS 4 ＋ 動詞 12） |
| 列距 | 61~70px | `RowPitch 64 = 16U` |
| 右邊距 | 21px | `Margin 24 = 6U`（就近落網格） |
| 右緣三列的右緣極差 | **0px** | 單一對齊軸確認 |

**`KeycapH` 從 19 改成 32**——19 是我猜的，實測他們是 1.6 倍。
字級階梯 12／16／24／36 ＝ 3U／4U／6U／9U（倍率 1.33／1.5／1.5）。

### 已落地（編譯 Succeeded）

- `DrawKeycap`：尺寸全部改讀 `NiUi`；鍵名字級 Small→Body（32 的鍵帽塞 12 的字＝全是內距）
- `DrawControlStrip`：兩欄 → **G2 疊放**（glyph 上／動詞下、共用右緣）
- **動詞填色 `PaperDim` → `Paper`**

### 一條要更正的既有敘述

我 09-04 說「改成 Meccha 那樣帶陰影的白字」，暗示我們沒有陰影——**錯的**。
`bTokShadows` 預設就是 `true`（黑 0.6、1px），連阿拉伯整形路都掛了。
**壞的一直是填色**（`PaperDim` 對膚色 1.41），不是缺陰影。
（同族：先量再宣稱，不要用「他們有我們沒有」的敘事套上去。）

### 待做（依序）

1. 滑鼠 glyph 貼圖進 UE（Kenney CC0 → `Content/UI/Input/`），`LMB/RMB/SCROLL` 改圖
2. 上緣改無面板（大數字＋祈使句＋受害者），底部 chip 改無面板
3. 右下常駐規則塊
4. 其餘 8 個相位的操作提示與祈使句、9 個相位名進 13 語字串表
5. 選單側：半徑收斂、Quit 分色

---

## 八之五、施工完成報告（2026-09-04，待 user viewport 驗收）

**user 指令**：「照抄 Meccha 的設計語言」→「全部完成並都有截圖量測清楚確認無誤後再讓我驗收」。

### 做完的（編譯 Succeeded、閘門全綠）

| # | 項目 | 證據 |
|---|---|---|
| 1 | **尺度正本 `NiUi`**：基準 U=4px，字級 12/16/24/36＝3U/4U/6U/9U | `hud_mock.py` 尺度閘門 PASS |
| 2 | **鍵帽 19→32px**（量 Meccha 實物來的，不是我猜的） | 截圖實測 glyph 高度 [32,32,32,32,32] |
| 3 | **操作列改 G2 疊放**（glyph 上／動詞下、共用右緣） | 截圖實測右緣極差 **0px**、列距 **[64,64,64,64]** |
| 4 | **滑鼠改 CC0 圖示**（Kenney，調色盤烘進 PNG、裁到 alpha 邊界） | ~~`/Game/UI/Input/T_InMouse*`~~ → 09-06 曾退役、**09-09 回到這條路**：Kenney 線稿版、資產改 `T_Ico_mouse_*`（§15.9） |
| 5 | **動詞填色 PaperDim→Paper**（對膚色 1.41＝隱形的 bug） | 截圖實測動詞 vs 陰影 15.07 |
| 6 | **上緣／底部 chip／姿勢面板拆面板** | 截圖實測背景標準差 23~45（有面板會壓到 ~0） |
| 7 | **鍵帽與 glyph 補陰影** | 白色障子牆前的實測（見下） |
| 8 | **右下角常駐規則塊**（相位名＋兩行怎麼贏） | 三張截圖都有 |
| 9 | **每個相位一句祈使句**、**八個相位補上操作列** | `ink him`／`lean in to his body`／`trace the line to the end` |
| 10 | **64 個新字串鍵 × 13 語**（相位名／祈使句／動詞／規則） | `hud_loc_lint` 守備擴到**全檔**、violations=0 |
| 11 | 選單：**半徑 9 種→3 種**、Quit 給 `DangerStyle` | `ui_token_audit` 兩項 PASS |
| 12 | 面板 alpha 7 種→3 種（0.72 次級／0.88 模態／0.6 dev） | 同上 |

**規則定案**：**常駐 chrome 無面板**（靠陰影），**模態才有面板**（墨杯盤／指認／兇手轉盤
——Meccha 的 picker 同理）；面板顏色**取背景反相**。

### 儀器（新增）

- `Tools/RoboTest/robo_ui_shots.py`：真機三態截圖（大廳／站著／鎖定）
- `Tools/UiCheck/shot_measure.py`：**量截圖**的版面不變量（邊距／單一右緣／列距／等高／
  無面板／對比），含**負向測試**
- `Tools/UiCheck/ui_token_audit.py`：全站樣式常數稽核
- `Tools/UiCheck/hud_mock.py`：全畫面離線 mock ＋尺度閘門

### 這一輪的方法論血價（四條，都是閘門自己出的錯）

1. **稽核工具的正則在 `WithA(...)` 的內層逗號就停**⇒ 把 alpha 當半徑，報出 13 種假數字。
2. **`rindex("	};")` 定位**⇒ 36 列字串插進了 `GCultureCodes`。**定位要用語意錨點。**
3. **群組化用了同一個 list 參照**（`append` 後 `clear`）⇒ 列距全報 0。
4. **亮度門檻 >210 把鍵帽整個濾掉**（Paper 的藍通道 α0.94 後只剩 208）⇒ 只量到純白的滑鼠圖，
   得到「三項全 PASS」的假通過。
5. **位置檢查第一版是空洞契約**：只驗「有邊界對比」，把期望邊距改成 120（指到白牆上）
   它照樣通過。改成驗**兩個族群同時存在**（紙色 chrome ＋ 深色墨），負向測試才開火。

**真機截圖也抓到一個真缺陷**：拆面板之後只有**文字**有陰影、**鍵帽沒有**，
站在道場白色障子門前 Paper 鍵帽幾乎融進牆（量測程式當時連鍵帽和牆都分不出來）。已補。

### 待 user viewport 驗收

證據＝`Saved/Screenshots/WindowsEditor/uishot_*.png`（三態）＋`Saved/UiMock/shot_dream.png`
（沉睡）＋`menu_root2*.png`／`menu_settings*.png`（選單）。
**幾何與對比已量測，手感與美感以 user 的 viewport 為準（鐵律一）。**

---

## 九、法務邊界（**抄之前先分清楚在抄什麼**）

> 我不是律師，這一節是工程上的操作邊界，不是法律意見。真要保險，上架前把最終截圖
> 給 IP 律師看一次是便宜的保險。

**著作權保護的是「表達」，不保護「想法／功能／慣例」。** 所以同一句「照抄」要拆成兩件事：

| 抄的東西 | 類別 | 判斷 |
|---|---|---|
| 右緣縱列、常駐、隨情境增減 | 版面**慣例** | 安全。這是整個品類的通用作法，不是誰的財產 |
| `[圖示][glyph 序列] 動詞` 的**文法** | **系統／方法** | 安全。著作權明文不及於 method of operation |
| 鍵帽＝圓角方塊＋鍵名 | 慣例 | 安全（Blender／Unity／大量遊戲都是這樣畫鍵） |
| 「滑鼠畫成滑鼠、按鈕點紅」的**概念** | 慣例 | 安全 |
| 右下角「模式名＋兩行怎麼贏」的**結構** | 慣例 | 安全（派對遊戲通用） |
| **他們的圖示檔案／滑鼠圖／沙漏圖** | **表達** | **不可用。自己畫。** |
| **他們的字串**（`Toggle X-Ray Rendering`／`隠れつづけよう`） | **表達** | **不可用**——而且我們的規則不同，抄了也是錯的 |
| 從 `.pak`／`.ucas` **抽出任何資產** | **表達＋契約** | **絕對不做。** 研究期讀過資產索引（檔名）＝調查手段；**任何抽出物都不得進 repo、不得進包** |
| 他們用的字型 | 授權 | `MPLUSRounded1c`／`ZenKakuGothicNew` 是 OFL，**要用就從 Google Fonts 合法取得**，不從他們的包裡拿。（但我們 08-06 已定案自己的字體矩陣，沒有理由換） |

**trade dress（商業外觀）**要成立，需要「有識別性」＋「會造成混淆」。近白鍵帽＋深字是
業界標準、不具識別性；而我們的畫面是紙／墨／相撲道場，與他們的螢光綠躲貓貓不會混淆。
**真正會踩到的是「整體抄成一個 clone」**——那不在我們的路徑上（不同品類、不同玩法）。

**可攜結論：抄文法，不抄字形。** 圖示一律用我們自己的美術語言重畫（墨線／筆觸），
這同時是法務要求與美術要求——**照搬他們的白色扁平圖示到紙墨介面上本來就會很怪。**

---

## 十、2026-09-05：全站對齊 Meccha（局內狀態系統＋選單改制；待 user viewport 驗收）

**user 指令**：「要在合法的範圍內全面照抄 Meccha Chameleon 的UI設計語言…依情況做最小限度的必要調整」→「全部完成並都有截圖量測清楚確認無誤後再讓我驗收」。

### 10.1 這一輪最重要的一件事：第一次看到他們的**選單**

此前全專案（含 §1 的「A 級證據」）只有他們**局內**的截圖。09-05 從 Steam 啟動實機，
拍到主選單／設定／開房／找房四個頁面——**而我們的選單與他們每一根軸都相反**：

| 軸 | Meccha | 我們（09-05 之前） |
|---|---|---|
| 主按鈕 | 左對齊、畫面左下 | 置中 |
| 按鈕樣式 | 深底＋**綠色外框** | **實心酒金** |
| 容器 | **完全沒有卡片** | 毛玻璃白卡 α0.78 |
| 背景 | 滿版即時 3D 場景 | 純黑＋一隻力士 |
| 返回 | 每頁左下 `[ESC] 返回` 鍵帽 | 純滑鼠、零鍵帽 |
| 特殊資訊 | 房號放在整條洋紅色塊上 | 明朝體大字 |

**真因不是執行失誤，是參考圖從來沒被打開過**：08-06 定「Meccha 式」時只有他們局內的
白鍵帽深字，從那裡往回推 ⇒ 推成白卡墨字＝他們選單的正對面。
**同族血價**：§1.5「我用畫面中央的一塊判決了整個畫面」、09-03「十張截圖不是他們的 UI」。

### 10.2 抄的是**規則**不是**值**（本輪的總方法）

§9 的「抄文法，不抄字形」原本只用在法務上。它其實是整件事的總方法，而且更嚴格一層：
**每條設計決定都是（規則，值），值都有前提；前提不成立時抄值＝把別人的答案抄到自己的考卷上。**

| 他們的做法 | 搬過來的**規則** | 我們填的**值** |
|---|---|---|
| 全站單一圓體、零襯線 | 一款遊戲只有一種字體人格，兩個載體共用 | **明朝體推進局內**（不是把選單的拿掉） |
| 綠＝確認／開啟／當前 | 強調色每軌一個語義、兩載體共用同一組 | **酒金**；局內那個綠是孤兒（選單 0 次）已拆 |
| 深底＋綠外框 | 外框承載強調色、填色保持中性 | 酒金外框＋墨色填底 |
| 常駐 chrome 無面板 | 常駐＝無邊界；模態＝有面板 | **前提不成立**，見 10.3 |
| 沙漏會排空 | 狀態用形狀講，不要只用數字講 | 罰酒杯 |
| 白／灰／黃／綠膠囊四態鍵帽 | 一個視覺通道一個語義；狀態改變形狀 | **三態**（無切換軸，見 10.4） |

### 10.3 唯一一條**不能照抄**的：他們的世界是暗的

「常駐 chrome 無面板」成立的前提是背景亮度有上限。實測：

    Meccha 的 chrome 背景亮度：27 / 80 / 84 / 155（最亮是打光的綠地板）
    我們的道場障子牆：        208  ⇒ 上緣祈使句對比 **1.27**（近乎隱形）

我原本假設「他們白字的描邊比較強、照抄就好」——**實測推翻**：他們字邊比背景暗
1.7~27 階，我們 7.7~10，同一個量級。**差的不是手法，是前提。**

⇒ 自己發明但守住規則的意思：**上緣一道沒有邊界的漸層壓暗**（`DrawTopScrim`）。
面板有邊界所以它是一個東西，漸層沒有邊界所以它是光線。

**兩次自己抓到的錯**（都寫進程式碼註解，不要再犯）：
1. 一版讓 alpha 從頂端 smoothstep 收到底 ⇒ **最暗的地方在沒有字的最上緣**，字所在處
   只剩 0.06；對比 1.27 只修到 1.89（仍不及格）。**地是給字用的，不是給邊緣用的**
   ⇒ 內容區維持峰值、只有內容下方才淡出。
   *教訓：閘門量到「有改善」不等於「修好了」——要對著門檻看，不是對著改善量看。*
2. 二版用 48 個 `DrawRect` 疊漸層 ⇒ robo 抓到 `cruise tipSpd` **2.05**（上限 2.00、
   基線 1.97）。改成 **1×64 貼圖一次 `DrawTile`**：視覺更平滑、成本回到常數。
   *這是同一個坑的第三次（09-03 兩次）：每幀成本會沿針的離散步進洩漏進手感。*

### 10.4 局內：狀態系統與版位紀律

- **鍵位四態 → 我們只做三態**（`ENiKeyState`）：可用／不可用／一次性。
  **`ToggleOn` 刻意不做**——雷射與搖夢都是一次性、裝睡是按住不是切換，
  §4.2「軸不存在就不該存在」同樣適用於狀態通道本身。
  真消費者：搖夢買不起＝灰、大廳人數不足＝ ENTER 灰（判準與底部那行字同源
  `CanHostStartMatch`，同一個判準不得寫兩份）。**不可用時鍵帽／glyph／動詞一起淡**。
- **`FNiControlHint` 單一正本**（§4.3 的第一號工程，已落地）：右緣與底部讀同一份，
  差別只在 `bPosture`。建它的理由不是好看，是**沒有它，手把支援或改鍵一到就全站重寫**。
- **兩個操作群**（他們有、我們此前只有一個）：右緣＝模式與動作（直排）、
  **底部中央＝姿勢與移動（橫排）**。`WASD 起身` 從右緣搬到底部。
- **右下角分時共用**：`GetVictimPenaltyCups() == 0` ⇒ 規則塊（新玩家正需要）；
  `> 0` ⇒ **比分**。**罰酒杯就是我們的 `残り人数`**（三杯結束這一局），
  此前是三個 20px 圖示擠在受害者名字旁邊。上緣那份已拆＝一件事只講一次。
- **巡禮不再冒用指認的名字**：此前 Tour 與 Accusation 共用文案 ⇒ 上緣寫
  「look at every piece」而右下寫「ACCUSATION」＝一張畫面掛兩個相位名。
- **中心色票方塊拆除**：它此前活在 8/10 個相位（大廳／巡禮／判決都有），
  而 §3 說 0° 區只准放「墨會落在哪」。細十字留著（他們也有），
  顏色改 Paper——09-03 已學過「指示器不准用內容的顏色」。

### 10.5 選單：無卡片、左對齊、外框化

- 白卡拆法＝**底換全透明、模糊歸零，版面樹一個字沒動**（`NiMenuCardBlur`／
  `PageGroundBrush` 兩個常數）⇒ user 要退回毛玻璃白卡，改那兩個值就回去了。
- **拆掉卡片才看得見的問題**：白卡此前替內容擋住背景那隻力士；卡一拆，置中的內容
  就直接疊在皮膚上（個人檔案頁的隱私聲明有一半在皮膚上）。**正解不是把卡片加回來，
  是把內容欄搬到標題那條軸上**——他們的版面本來就是內容靠左、場景佔右。
- **`[ESC] 返回` 左下**，且**可點**：它既是提示也是按鈕。頁面內原本那 6 顆 Back 全部
  拆除，`GoBack()` 成為返回的唯一實作（此前 ESC 與滑鼠是兩條路）。
- 房碼給**一整塊酒金實填＋墨字**：它是整個 UI 裡唯一玩家必須唸給朋友聽的東西。
- 欄位計數器 `n/16`（上限此前只在打字被無聲截斷時才會被發現）。

### 10.6 儀器（新增／修好）

| 工具 | 守什麼 |
|---|---|
| `Tools/UiCheck/phase_consistency.py` | **跨相位**版面（既有 `shot_measure` 只量一張圖之內）＋上緣對比 |
| `Tools/UiCheck/menu_measure.py` | 選單六頁：單一左緣軸／無卡片／ESC 鍵帽／對比／內容不壓力士；**含負向測試** |
| `Tools/RoboTest/robo_ui_shots_all.py` | 跑完整局沿路截圖（既有的只拍三態） |
| `Tools/RoboTest/menu_shots.ps1` | 選單六頁實機截圖（一頁一次啟動——ExecCmds 是同時執行的） |

### 10.7 這一輪的方法論血價（**四條都是閘門自己在說謊**）

1. **`ui_token_audit` 的 ③④⑤ 是寫死的 `print`，不是量測**。⑤ 引用的
   `CAP_H 19 / ROW_H 26` 在 09-04 立 `NiUi` 之後就不存在了，而它照樣每輪印 FAIL。
   ④ 寫死一句「局內沒有 serif 旗標」，於是明朝體接進局內之後它照樣報 FAIL。
   **閘門抄一份結論在自己身上，那份一定會過期**——這是本專案第四次踩。
2. **`/**墨**` 被當成塊註解開頭**：`ui_coverage`／`ui_token_audit` 的 `strip_comments`
   先剝 `/* */` 再剝 `//`，而 HUD 裡有一行行註解寫著 `/**墨**＝地對比…`
   ⇒ naive 正則一路吃到下一個 `*/`，**實測吞掉 10,918 個字元**，剛好蓋住
   `BuildControlHints` ⇒ 操作列整欄報 0/14。**既有的潛伏 bug，只是這次被吞的範圍
   剛好蓋到要讀的函式。** 修＝先剝行註解再剝塊註解。
3. **`phase_consistency` 找的是舊的綠色**：規則塊標題改酒金之後它報「規則塊 0/10」
   而畫面上明明有。同型第二次。
4. **負向測試選錯受測物**：我拿「修改前」的設定頁去測「內容壓到力士」，量到 1px
   ——因為修改前**有白卡，而白卡正是用來擋住力士的**。真正的失效模式是
   **拆掉卡片之後內容還留在中央**。改成拿現在的截圖、把取樣窗移回舊的置中位置，
   量到 123,726 px（門檻 8,000）＝閘門真的有解析度。
   *可攜：負向測試要打中「你這次修的那個失效模式」，不是「修之前的那張圖」。*

### 10.8 量測結果（截圖證據＝`Saved/Screenshots/WindowsEditor/uiall_*.png`、`menu2_*.png`）

- 上緣祈使句對比：Tour **1.27 → 4.55**、Accusation **1.30 → 4.62**、
  Lobby 2.33 → 6.70、Seating 2.37 → 6.60、Drawing 鎖定 2.75 → 7.31（全數過 WCAG 4.5）
- 右緣單一對齊軸：10 個相位極差 **0px**（維持）
- 中心色票方塊：**8/10 → 0/10** 個相位
- 規則塊：7/10（與原始碼逐項相符；Lobby／Seating／Resolution 設計上沒有）
- 選單六頁：`menu_measure` **FAILS 0**，標題左緣極差 5px（殘差＝字腔側距，
  版面軸六頁同一個常數，由原始碼檢查守）
- 迴歸 `robo_directdraw`：**60 PASS / 5 FAIL**，失敗集是基線已知集合的子集
  （基線 59/6）；`cruise tipSpd` 1.96（基線 1.97）
- `hud_loc_lint` violations=0（守備全檔）／`tray_layout` 233/0／`tray_cursor` 15/0
  ／`hud_mock` 尺度閘門 PASS

**幾何與對比已量測；手感與美感以 user 的 viewport 為準（鐵律一）。**

### 10.9 記帳（刻意未做／待裁）

- **局內裸數字 158 個、87 個不在 4px 網格上**：`NiUi` 的 7 個常數全在網格上，
  其餘是 09-04 之前的舊尺寸。併入會動到已驗收的畫面 ⇒ **待 user 點頭**。
- **設定頁的左側分頁欄不做**：他們光「圖像」就 14 列所以需要分頁；我們 6 列一頁裝得下，
  §4.2「軸不存在就不該存在」同理——分頁是為了切分長清單而存在。
- **象形圖欄不做**：他們右緣每列右側有一個圓形線稿圖解。那需要自己畫美術
  （§9：圖示一律用我們的墨線重畫，不可用他們的檔案），且 §4.1 已定「動作圖示是選配」。
- 對局內的**大廳／ESC 選單／結算**三個面仍無截圖（要開一場真房；建房需要打字，
  而本輪鍵盤注入被權限擋掉）。
- 相位覆蓋：祈使句 12/14、操作列 7/14、規則塊 6/14——判決／結局兩個純演出面
  刻意留空（演出自己會說話）。

### 10.10 二修（2026-09-05，user viewport 打回：「這是什麼排版？」）

一修我把 `HAlign` 從置中改成靠左，**卻沒有改那個為置中而設計的構圖**。實測後果：

    左緣三種：75（標題）／106（主按鈕）／80（底列）＝極差 31px
    垂直空洞：310px ＋ 322px ＝ 螢幕高度的 58% 是島與島之間的空白

**三個病因，全部是「舊制度的殘骸留在新制度裡」**：

1. **106 = 72 + `NiSpace::CardPadH`(32)** ——那是**白卡的內距**。內距是給「卡的邊」
   用的，沒有卡就沒有邊要閃，它變成一個沒有理由的縮排。（已歸零＝`NiMenuGroundPad`）
2. **三帶構圖**（標題頂／卡置中／底列貼底）是為了「中間那張卡」而生的。卡不見了，
   中間那一帶就只剩兩個 `FillHeight` 撐出來的空洞。**已改成一疊**：主按鈕、狀態行、
   次要動作列收在左下同一條軸上（Meccha 的首頁就是這樣）。
3. **`SwayCm = 180` 的理由寫在它自己的舊註解裡——「每趟穿過毛玻璃卡後面」**。
   卡拆了，那個理由不存在，而值留著 ⇒ 力士走到畫面左半，設定頁的數值疊在他皮膚上。
   **新制＝內容靠左、場景佔右，由舞台自己遵守**：從幾何反推（內容欄右緣 x=632 →
   world Y +79.3cm、體半寬 ~84.6cm）得 `StageBiasCm = -76`、`SwayCm = 71`。

連帶修掉：底帶未替左下 `[ESC] Back` 讓位 ⇒ 設定頁的 Licenses 與它疊在一起
（`NiMenuBottomReserve = 56`）。

**可攜鐵則：拆掉一個東西的時候，要一起拆掉「為了它而存在」的所有常數。**
白卡拆了，它的內距、它撐出來的構圖、為了繞過它而設的舞台幅度——三個都還在，
而它們各自都曾經是對的。

### 10.11 我的閘門為什麼沒擋住（**這一節比修法值錢**）

一修的 `menu_measure` 報「單一左緣軸 PASS」，而 user 的 viewport 上一眼就看得到三條
左緣。原因：**它只比了「六頁之間的**標題**左緣」，對「同一頁之內」完全沒有解析度。**
這正是本文件 §7 那條鐵則要問的：**什麼樣的壞會讓它照樣亮綠燈？**

補的過程本身又踩了三次同型的坑，值得記：

- **顏色啟發式三版都不穩**：`lit`（不是黑）⇒ 力士被算成版面；`lit & ~skin` ⇒ 他身上
  **過曝的高光**逃出膚色述詞；改用調色盤 ⇒ 深底按鈕只被看見**裡面的字**（按鈕框太暗）。
  三個症狀互斥 ⇒ **我在錯的層**。對齊軸是**版面事實**不是影像事實：
  **判準搬去讀原始碼**（頂層 slot 的左內距只能有一個值、`CardPadH` 不得殘留），
  像素那一版降級為佐證。負向測試：同一組判準套在 HEAD 上 ⇒ 開火（29 處置中殘留＋CardPadH）。
- **膚色述詞把反鋸齒後的 Paper 文字算成皮膚**（Paper 邊緣 (206,197,182) 剛好落進述詞）
  ⇒ 量到「力士最左 x=72」，結論整個反過來。改成**要求一欄有 150 列以上連續膚色**
  （力士是實心大塊，文字最高 40px 又是鏤空的）。
  **述詞要能分開你要分開的那兩類東西，否則它只是在回答別的問題。**

### 10.12 二修後的量測

| 量 | 一修（被打回） | 二修 |
|---|---|---|
| 首頁頁內左緣極差 | **31px** | **3px** |
| host／settings／language／profile 頁內極差 | 34／32／38／32px | 4／2／6／2px |
| 主按鈕與次要動作列的垂直空隙 | **322px** | 80px（一疊） |
| 力士最左 x vs 內容欄右緣 632 | 重疊 | **+373~382px（六頁全清）** |
| `menu_measure` | （無此檢查） | **FAILS 0**（30 項 PASS） |

其餘閘門複驗：`hud_loc_lint` violations=0／`tray_layout` 233/0／`tray_cursor` 15/0／
`hud_mock` 尺度 PASS／局內跨相位右緣極差 0px、中心色票 0/10。

*10.10~10.12 建立 2026-09-05（二修）。*

*十、節建立 2026-09-05（二修同日）。與實作對賬日期：2026-09-05。*

---

## 十一、2026-09-05：中性制（user 作廢 08-06 白卡明朝制；本節取代 §二～§三的載體與顏色描述）

**user 定案（原話）**：「我其實只是想要一個成熟、適合的 UIUX 系統、設計，要讓這個遊戲看起來
像是大廠的設計，而不是像現在一樣的半成品樣……中性、簡約、通用的設計就足夠了，不需要太多
花裡胡哨的設計，讓遊戲內容站出來」→「我打算作廢，改成簡約中性的設計」→「可以的話就開始」。

### 11.1 診斷：半成品感的四個來源（不是設計語言）

1. **留白是用「什麼都沒有」做的**：選單＝黑虛空加一隻力士。簡約與未完成的差別在於留白是
   用什麼做的；大廠的簡約選單後面永遠有一個被構圖過的世界。
2. **系統只蓋到六成**：祈使句 12/14、操作列 7/14、規則塊 6/14。做好的畫面各自都好，
   整個產品仍讀成半成品——大廠的樣子九成是「沒有例外」。
3. **零動態**：選單與 HUD 的轉場、淡入、hover 動畫**一行都沒有**（實測 grep）。靜態截圖
   再好，動起來的第一秒就被讀成 mockup；而驗收一直只看靜態截圖，所以看不見。
4. **口吻不一致**：Title Case 與全小寫並存、大廳浮著一塊 debug 表、「文A」孤立圖形鈕、
   上緣壓暗讀成灰霧。

**08-06 那套（紙／墨／酒金／明朝）不是中性**：暖 UI 疊暖世界（皮膚、木頭、榻榻米）分離度
天生不夠（酒金動詞對皮膚 1.41、紙色對障子牆 1.27），而且它在說「日本、紙、墨」——世界
已經用相撲、和彫、障子說過一次，UI 再說是重複不是襯托。大廠讓內容站出來的手法幾乎都是
反過來的：**世界有色、UI 無色**。

### 11.2 系統（正本＝`NiceInkUiTokens.h`）

| 軸 | 值 | 規則 |
|---|---|---|
| 顏色 | 白 242（四級透明度 100／70／45／25）、黑 10、強調色＝稿筆結晶紫（填 `Accent` 124,66,168／字 `AccentText` 190,150,232）、紅 224,82,70 | **強調色只准畫在深色面上，永遠不直接落在世界上**；成功＝白、失敗＝紅 |
| 字體 | Noto Sans 一族（Latin／JP／TC／SC／KR／Arabic，OFL） | 13 語同一家；明朝與圓體全退（Serif 席位映到無襯線，舊呼叫點不改） |
| 字級 | 14 標籤／18 內文與動詞／28 標題與祈使句／64 主角數字 | 四階、兩種字重；相鄰身分至少差兩軸 |
| 間距 | 4px 網格；鍵帽 32／列距 64／邊距 24 | 沿用（量 Meccha 得來的是尺度不是風格） |
| 圓角 | 4px 一個值 | 面板、chip、鍵帽同值 |
| 面 | 常駐無面板；模態＝黑 60%；上緣文字後方一道無邊界漸層（黑 50%→0） | 面板有邊界所以是一個東西，漸層沒有所以是光線 |
| 動態 | 相位切換 chrome 淡入 0.25s／換頁淡入 0.2s／hover 即時 | 全站同一條 smoothstep |

**八個元件**：祈使句、鍵帽＋動詞、臉章（臉＋名字）、主鈕（強調色實填＋白字，一頁一顆）、
文字鈕（無底無框、hover 轉強調色＋左側短棒）、chip（選中＝強調色實填）、清單列、模態面板。
表以外的東西不准畫。

### 11.3 每個面回答的問題（設計從這張表推，不從參照推）

| 面 | 玩家在問 | 畫面上只能有 |
|---|---|---|
| 主選單 | 我要開房還是加入 | 左欄一疊文字：兩個主動詞（28）、三個次動詞（18／70%）、13 語 chip；右側＝道場裡跳舞的力士 |
| 大廳 | 房號是什麼、誰來了、什麼時候開始 | 房號 64 級在祈使句下（同一片漸層）；底部一排臉（空位＝黑 25% 淡框）；一行計數；ENTER 在右緣、人數不足灰 |
| 儀式 | 誰要睡了 | 零 chrome，鏡頭自己回答 |
| 作畫者 | 我拿什麼筆、他還有多久醒、我能按什麼 | 針尖圓環、一枚墨杯 chip、倒數、右緣動詞；第一局右下規則塊，之後換杯子 |
| 沉睡者 | 描到哪了、誰在搖我 | 描圖盤、進度、搖晃者的臉 |
| 巡禮 | 還有多久、看清楚每一件 | 倒數、祈使句 |
| 指認 | 這件是誰畫的 | 底部一排候選的臉；當前嫌疑人＝黑 60% 底＋強調色底線＋強調文字名字 |
| 判決 | 對了嗎、罰幾杯 | 臉、對／錯（白／紅）、杯子 |
| 局末 | 誰身上留了什麼 | 力士本人，鏡頭走一圈 |

三條由此推出的規則：**臉是身分的唯一載體**（名字是輔助）；**現金與罰酒杯是全站僅有的
兩個數字**；**畫面正中央只准有針尖圓環**。

### 11.4 這一輪改了什麼

- `NiceInkUiTokens.h`：整張表換掉（舊名保留為別名；`NiSpace::CardPadH` 等歸零）。
- 字體：`Tools/AssetPrep/ue_import_noto_sans.py` 匯入 12 面（`SourceAssets/Fonts/NotoSans/`，
  OFL 全文同目錄）；`BuildCompositeUiFont` 矩陣改 Noto Sans（TC／SC 靠 culture 分流、
  KR／JP／Arabic 碼域、fallback＝JP）。
- 局內：大廳面板拆除（房號進上緣主角數字、席位改底部一排臉）；指認面板拆除（一排臉）；
  落在世界上的強調色全改白；主鈕／一次性鍵帽＝強調色實填＋白字；相位切換淡入
  （`ChromeAlphaBase`，掛在原語上）；ESC 選單底＝黑 60%。
- 選單：樣式庫整套換（圓角 4、chip 選中強調色實填、房碼格改深地淡格＋白字＝修掉此前
  紙字疊紙格的隱形 bug）；首頁改文字鈕堆疊＋13 語 chip（`MakeTextButton`／
  `MakeRootLangRow`）；換頁淡入（`PageRoots`＋`Tick`）；授權頁改左欄。
- 舞台：`NiceInkMenuStage` 以 `LoadLevelInstance` 串入 L_Dojo，舞台原點搬到榻榻米中心
  （430,40）、曝光用道場承重值 5.2、平行光只在退路上點。

### 11.5 驗收（規則：對著畫面看，不對著自己的規則量）

- 靜態：`menu_shots.ps1` 六頁＋`robo_ui_shots_all.py` 全相位——只看「表以外有沒有東西」
  與「每個面是否只回答那一兩個問題」。
- 動態：60 秒流程影片（ffmpeg gdigrab 錄 -game 視窗、robo 鉤子驅動、不注入輸入）。
- 最終裁判＝五個不懂設計的朋友看兩支影片：「哪一個像正式上架的遊戲」。
- 自製的像素度量（左緣極差、對比公式）降級為佐證，不再當閘門——它們量的是自己的規則。

*十一、節建立 2026-09-05。與實作對賬日期：2026-09-05。*

### 11.6 第一輪截圖結果（2026-09-05；證據＝`Saved/Screenshots/WindowsEditor/menu2_*00002.png`、`uiall_*.png`；前後對照＝`Saved/UiMock/neutral_before_after_0905.png`）

- 選單六頁：道場實景一次載入成功；**第一張截圖就抓到三個問題**——白障子牆前白字隱形
  （預測中的：他們的世界暗、我們的亮）⇒ MenuHUD 在 Slate 之下畫左側 58% 寬的無邊界暗漸層；
  力士懸空（踩在 z=0 的隱形箱上）⇒ 道場載入時關箱碰撞；「简」豆腐（en 文化下 Han 碼域先命中
  日文面）⇒ fallback 改簡中面。二修後六頁全部可讀、同一條左緣軸、同一組元件。
- 局內（robo 兩角色、19 張）：大廳一排臉＋空位框、上緣祈使句、右緣鍵帽（ENTER 人數不足灰）；
  作畫者右緣四個動詞、G 一次性＝強調色鍵帽；巡禮倒數 64 級；指認（本人）＝候選臉列＋黑底
  強調底線；判決 CORRECT 白／WRONG 紅；罰酒杯右下（第二杯起空杯紅）。
  一個小缺陷：大廳房主名字後綴「· host」在 72px 預算下截斷 ⇒ 改粗體名字（三修）。
- 未做／待裁：流程影片（`Tools/RoboTest/record_window.ps1` 已備、未錄）；ESC 選單與結算面
  無截圖；上緣漸層在障子牆前讀成灰帶（0.50，可調）；PIE 大廳無房號（正式流程才有）。

### 11.7 第二輪（2026-09-06）：補拍與影片的結果

- robo 鉤子新增 `DebugRoboFinale`（GameMode）與 `DebugRoboSystemMenu`（Character）；
  `robo_ui_shots_all.py` 延長到 ESC 選單／結局／場間。結局拍到（`uiall_art_10_finale`），
  並抓到一個真缺陷：沉睡黑屏上的 OUT COLD 與副標疊在一起（副標偏移寫死 52，主標已是 64 級）
  ⇒ 改讀主標實際高度。場間（PostGame）因 Finale 30 秒超過等待逾時未拍到；ESC 選單那張
  鉤子有呼叫、截圖裡沒有選單，原因未查（記帳）。大廳房主名字改粗體後已重拍確認。
- **流程影片不可行（user 在機時）**：ffmpeg gdigrab 錄「視窗」實際錄到的是那塊螢幕區域
  ——遊戲視窗被 VS Code 蓋住，錄下來的是 user 的桌面。影片已即刻刪除。
  **規則：user 在機時不做任何螢幕錄影**；影片要嘛由 user 自己錄，要嘛等引擎內
  （MovieRenderQueue／逐幀 HighResShot）方案，不走桌面擷取。

### 11.8 鍵帽二版（2026-09-06；user：「要怎麼讓玩家一眼看出這是在講按鍵？我就看不太出來」）

一版的鍵帽＝白色實心圓角塊＋黑字＋硬陰影，讀成標籤或貼紙。「這是鍵盤上的一顆鍵」的通用符號
是**深色半透明底＋1px 亮邊框**，二版照此重畫；三個連帶：
- **鍵與動詞排成一句**：右緣「動詞 [鍵]」橫排、鍵貼右緣、列距 44；底部「[鍵] 動詞」橫排。
  一版圖在上字在下，兩者被讀成兩個獨立物件。
- **滑鼠自己畫**（`DrawMouseGlyph`）：與鍵帽同底同框的膠囊、中線分兩鍵、按下的鍵填強調色、
  滾輪＝中線小膠囊。Kenney 剪影退役（線條不同家，且按鍵是紅色＝第二個強調色）。
- 一次性動作＝紫框＋紫 45% 底（不再整顆飽和）；不可用＝整顆淡。墨杯 chip 加 1px 白框；
  上緣壓暗 0.50→0.40；選單的 `[ESC] Back` 鍵帽同形。
證據＝`uiall_art_05_draw_locked.png`（09-06 08:25）。

### 11.9 強調色改清酒金（2026-09-06；user：「主色是怎麼選出來的……重新思考」→「去改」）

紫是我 30 秒內選的（「稿筆的顏色、冷色分得開」），站不住：它是稿線的墨色＝一色兩義，
且「必須是冷色」是推出來的規則不是量出來的。五款參照實測（`Saved/UiMock/ref_palette/`）：

| 遊戲 | UI 主色（實測） | 來源 |
|---|---|---|
| Meccha | 綠 #26CB0E／黃 #EFBE16／紫 #9804F5 | 品牌吉祥物（變色龍） |
| REPO | 螢光綠 #56EF5D／黃 #F7EA06／橘 #DE4C03 | 題材（CRT 終端機） |
| Among Us | 紅 #D62023（身分時刻）／綠 #22D823（任務） | 品牌（紅色船員） |
| PEAK | 綠 #57C303／琥珀 #C5952B／奶白 | 世界本身的插畫調色盤 |
| Liar's Bar | 金 #F1D37D／#F0CB5B、奶白鍵帽 | 世界的光（燈光、威士忌）調亮一階 |

**兩種策略：有品牌色用品牌色；沒有的從世界的光裡取一個調亮的顏色。沒有一款用互補色。**
暖世界配暖主色不糊，靠明度與位置（Liar's 金 v94 永遠在深底上）；08-06 酒金 v91 幾乎一樣，
敗在壓在同明度的皮膚上。Nice Ink 無品牌色 ⇒ 從光取：**酒**（每回合的儀式、唯一的比分）。
定案：`Accent` #D9B24A（填色、配黑字 `OnAccent`）、`AccentText` #F0CB5B（深底上的字與框）。

### 11.10 精緻化一輪（2026-09-06；user：「簡約中性但太不精緻、有些呆板」→「去自己截圖檢視所有 UI 畫面」）

全站重拍（選單六頁＋局內兩角色 21 張）逐張看，呆板的來源不是字體也不是中性，是四個「怎麼畫」：
大塊實心金色板子／字級對比太低／間距均勻無節奏／舞台正對無縱深。修：
- 金只當**線與字**（主鈕金框金字、hover 才微填；chip 選中白 14% 底＋金字、未選透明）
  ——Liar's Bar 的金從不填大面積。分段控制加 1px 外框容器。
- 頁標題 40 級細、標籤不粗；主鈕 18 級半粗、高 44、寬隨文字；子頁內容頂對齊（標題下 48px），
  拆掉垂直置中的撐開器（空洞是它撐出來的）。
- 設定頁：箭頭方塊改細箭號、每列一條 10% 髮線；語言頁：灰板網格改兩欄文字；房碼格改 1px 外框。
- 局內：揭曉／結局橫幅下的臉改接橫幅實際高度（此前疊在一起）；上緣壓暗 0.40→0.30、尾巴縮短。
- 舞台：相機移到 +Y 側朝 −Y 偏 14°（yaw 194；一版寫成 166 看向反方向、力士整個出畫面）、
  f/4 景深對焦力士。
證據＝`menu2_*00004/00005.png`、`menu7_*.png`、`uiall_art_*`（09-06 09:1x）。
未解：ESC 選單截圖仍拍不到選單（鉤子有呼叫）；一輪 robo 出現鎖定畫面全黑、下一輪正常＝暫時性，
未歸因。

### 11.11 主選單定型（2026-09-06；user：「為什麼還是很怪？大廠會怎麼設計？」）

大廠主選單的四個特徵，逐一落地：①一欄同尺寸的四到六個動詞、等距、Quit 前隔一段（此前兩種
字級＝網站導覽列）②語言不上主頁：13 語雲退場，改右下頁尾「文A 語言名」小選擇器（進語言頁）
③整張畫面壓一階：左帶之外補上／下／右三邊暗角，主角留亮④頁面有底：右下頁尾＝語言＋版本。
主頁與內頁同一條規則：內容從標題下 48px 開始。證據＝`menu10_root00000.png`、`menu10_host00000.png`。

### 11.12 主頁節奏（2026-09-06；user：「排版有點怪」）

兩個真因：①主頁行距三種（每列高度由內容決定＋collapsed 槽的殘留）②搜尋中 Host／Join 走
SButton 停用＝Slate 灰化，主動詞比次要還淡。修＝文字鈕固定行高（字級 ×2.25）、停用改點擊閘＋
45% 白；開房／加入頁主鈕寬＝控制欄寬（右緣對齊）。

## 十二、2026-09-06：「怎麼畫」第一批（全流程截圖 vs 六款參照的洞見落地；BUILT-自驗、待 viewport）

**起點**：user 問「為什麼我現在的 UIUX 感覺如此陽春簡陋（大學生 PPT／初學前端第一個網頁）」。
方法＝走完我們自己的 29 張全流程截圖，與六款派對遊戲（Meccha、REPO、PEAK、Among Us、Liar's Bar、
RV There Yet）的商店圖／預告片逐格／Game UI Database／wiki／**Meccha 與 REPO 實機**並排。
報告＝`Saved/UiMock/UIUX_INSIGHT_0906.html`；原圖＝`Saved/UiMock/ref/`；記憶＝project_ui_insight_0906。

### 12.1 診斷（一句）
**UI 不是太簡約，是沒有被製作過。** 四件事全沒做：字沒被排（Noto Sans Regular 預設值、小寫句子、
一種字重，全部落在 14～28 的「文件區間」）、形沒被畫（1px 金框＝Bootstrap outline、無標誌字、無圖示）、
世界沒被構圖（力士正面置中平光、障子過曝、左側壓暗漸層是繃帶）、狀態沒被演（零動態）。
「中性」被實作成「拿掉」。六款參照共通的不是風格，是製作紀律：標誌字必有／展示體＋內文體兩種人格／
三級尺度／一組圖示／面板只給模態、常駐字全有描邊／主鈕實心／世界被構圖／180ms 過渡／不解釋。
**REPO 的主選單是「簡約中性」的教科書；Meccha 的選單（彩虹標誌、綠框、語言雲）不該當中性參照。**

### 12.2 第一批落地（不動版面，只換「怎麼畫」）
- **展示字體 Oswald**（OFL；`SourceAssets/Fonts/Oswald/`，`Tools/AssetPrep/ue_import_oswald.py` headless 匯入
  `FF_Oswald_Medium/Bold`）住進複合字體的 Serif／SerifRegular／SerifBlack 三個槽位——所有「聲部」呼叫點
  （`bSerifFace`／`Role.bSerif`）自動換成展示體；CJK／阿拉伯子面的同名槽位仍是各自的 Bold。
  展示體一律 **ToUpper＋字距 50‰**（canvas 在 MeasureTok／DrawTok 內、Slate 在呼叫端 `Up()`）。
- **尺度三級**（`NiType`）：Small 13／Text 18／Hero 96；頁標題 48、動詞 24、相位與祈使句 32。28 與 36 刪除。
- **全站描邊**：Slate 每個 `STextBlock` 掛 (1,1) 黑 55% 陰影（59 處，機械替換）；canvas 本來就有。
- **主鈕實心白＋黑字**（停用＝白 35%）；次鈕白 14% 無框；鍵帽底 70%＋框 85%（Slate 與 canvas 同值）。
- **標誌字** `T_UI_Logo`（`Tools/AssetPrep/make_logo.py`：Oswald Bold 排字＋一滴墨，893×303 透明 PNG）：
  主選單左上 400px 寬；缺席時退回展示體文字。MenuHUD 以 UPROPERTY 錨住貼圖（Slate brush 不保 GC）。
- **解釋句拆除**：右下規則塊只留一行（L2 字串留表不畫）；開房頁「可見性後果」與加入頁「用鍵盤打」兩句刪除；
  祈使句改展示體 32 大寫＝這一面唯一的一句話。鍵帽旁的動詞改展示體 Medium 小型大寫。
- 授權：THIRD_PARTY_NOTICES.md 與選單授權頁補 Oswald。

### 12.3 坑
- **貼圖尺寸讀 `GetSurfaceWidth/Height`**：`GetSizeX/Y` 在未 cook 的 -game 下讓標誌被拉高 2.6×（第一張截圖抓到）。
- headless `-ExecutePythonScript` 的腳本路徑含空白＝「Could not load Python file」——複製到無空白路徑再跑（陷阱年鑑既有）。
- `Start-Process powershell -ArgumentList` 給陣列時，含空白的 `-File` 路徑會被拆＝腳本靜默不跑；要組成一個含引號的字串。
- Slate 主鈕停用態＝整顆疊灰（糊成一塊）⇒ 明寫 `SetDisabled`。

### 12.4 第二批：舞台與圖示（同日；user「請直接完成至結束」）
- **舞台**：曝光 5.2→4.2（整張壓一檔）、機位 108→92cm 微仰 1.5°（腰高＝主角感）、f/2.8→2.2、
  力士皮膚 `SkinBrightness` ×1.3 在 Tick 等 MID 出現後乘一次（1.75 把皮膚洗白＝實拍打回）、
  MenuHUD 三邊暗角 0.45／0.62／0.45。主頁動詞欄以 FillHeight 0.22／0.78 放到畫面高 ~34%（REPO 的欄位）。
  **做不到的**：暖色主光與四股循環——皮膚是材質內假光、道場 fullbright，真光打不上去；四股需要新的程序動畫，記帳。
- **圖示**（**⚠ 全部已於 2026-09-09 退役，見 §15.9；`lucide_icons.py` 已刪，以下為歷史**）：Lucide（ISC）用字型渲染 33 個 128px 線圖示（`Tools/AssetPrep/lucide_icons.py`＋`ue_import_icons.py`
  → `T_Ico_*`）。現金→coins、罰酒杯→wine（game-icons 的兩個退役＝一家圖示）；操作列每個動詞前 20px 圖示
  （`ActionIcon()` 動詞→圖示表；沒有對應就不畫）。

### 12.5 第三批：容器
- 設定頁改成**面板**（黑 62%、圓角 8、內距 24／16、背景模糊 8）、列標籤大寫小標（Label）。
- 大廳房碼從上緣中央改成**右上面板**（黑 62%、ROOM CODE 小標、房碼展示體 96、人數一行）——PIE 沒有房碼，
  正式流程才看得到，未截圖驗證。底部狀態句改展示體小型大寫。
- 醉夢描圖：黑紙→**靛藍地＋上下兩道無邊界暗帶**（ScrimTex 翻 UV），進度改展示體 96 放右上
  （一版放右下與規則塊疊在一起＝實拍打回）。
- 揭曉／結局：橫幅、臉、名字後方一道**全寬暗帶** `DrawRevealBand`（此前三層資訊直接壓在臉上）。

### 12.6 第四批：動態
- **相位橫幅**：換相位頭 1.4 秒祈使句以 96 級在畫面中央 0.25s 淡入、停、0.4s 淡出，上緣 32 級那句交叉接手
  （robo 在 next_round 那幀正好拍到「HE IS FALLING ASLEEP」置中）。
- 既有：相位 chrome 淡入 0.25s、換頁淡入 0.2s、落筆 chrome 淡出。**未做**：Slate hover 的顏色過渡（SButton 樣式切換是瞬時的）、
  按下縮放、數字跳動——記帳。

### 12.7 這一輪的坑
- robo 編輯器首次載入新貼圖啟動變慢，harness 8 秒就叫 PIE ⇒ `LevelEditorSubsystem` 為 None 整輪零圖；
  已改成等到子系統存在（上限 120s）。
- C4456（區域變數遮蔽）在本專案是 error：`Count` 已是 DrawTopBar 的倒數字串。
- 每個替換 assert 命中數的編輯腳本在一個 assert 失敗時已寫出前半檔案 ⇒ 重跑要 idempotent（分檔／加 marker）。

### 12.8 主頁 HOST／JOIN 忽暗（user：「為什麼有時候上面這兩個按鈕會變暗？」→「去從根本解決」）
病根兩層：①搜房是進加入頁自動發出的，ESC 回主頁不取消它，LAN 搜尋是引擎寫死的 5 秒 ⇒ 回主頁後主動詞暗上幾秒、
狀態列還寫著 looking for rooms；②主頁把三種忙碌（開房中／搜房中／加入中）一視同仁地鎖主動詞，而搜房中按 HOST 沒有衝突。
修＝`GoBack` 離開加入頁時取消前景搜尋（背景 12s 自動更新不動）＋主頁改用 `IsHostJoinLocked()`（只鎖開房中／加入中）。
驗證＝新 robo 鉤子 `NiMenuBack <秒>`（等同按 ESC，零輸入注入）：`NiMenuShowJoin,NiMenuBack 4,NiMenuShot 7 fixjoin_root` → log「menu action cancelled by user」＋主頁主動詞全白、無狀態列（SendKeys 的 ESC 打不到未聚焦的遊戲視窗＝改走鉤子）。

### 12.9 大廳二修（user 拿四人真局截圖問「這個排版是有設計過的嗎？」）
一修在四個真名下露出四個問題：名字預算 88 > 席距 72 ⇒ 名字互疊；房碼 96 級（倒數的尺寸）四個字母撐出畫面寬 1/5；
「4 / 6」13 級與房碼差七倍讀不成同一塊；底部「ENTER — START THE MATCH」與右緣 [ENTER] START 講同一件事。
二修定案：**身分列**＝臉 64、席距 112、名字預算＝席距−8（截斷加省略號）、貼底；空位＝1px 白 25% 框＋黑 25% 底。
**房碼面板**＝固定寬 320、第一列小標靠左（小視窗時截斷讓位）＋人數 18 級靠右、房碼 64 級置中、與現金列留 16。
**狀態句只在需要解釋時出現**（房主人數不足／非房主等房主），放在臉列上方；能開始時只剩右緣鍵帽。
驗證＝`Saved/UiMock/ref/lobby4_test.sh`：主機 AutoHost → 讀 log 房碼 → 三個沙箱客戶端 `NiMenuJoinCode` 加入 → `NiShot`
拍主機 1080p 與客戶端 540p（`lobby4_host_b`、`Saved_P2/.../lobby4_client_P2`）。零輸入注入。
坑：`NiMenuSetName` 的名字沒有落到客戶端的名冊（顯示的是平台名 Willie_desktop…）——與版面無關，記帳待查。

### 12.10 房碼抽到髒字（user 真局截圖：FCUM）
`MakeRoomCode` 只剔 I/L/O，四個隨機字母含母音就會拼出詞。修＝只用子音（BCDFGHJKMNPQRSTVWXYZ，20 個）＋短黑名單
（FCK／CNT／DCK／SHT／NGR／KKK／XXX…）重抽，64 次都撞才退回固定乾淨碼；20⁴＝16 萬組合。房碼仍要能唸：子音串
逐字母唸（B-R-T-K），本來就不是當單字唸。**大廳取景（第三人稱固定機位、力士站定位、名字掛頭上）待 user 點頭。**

### 12.11 房碼掉出面板（user：「這個地方是你的故意設計嗎？」→「我拍這麼多張你眼瞎看不到嗎？」）
面板高度用猜的行高常數（字級 ×1.18）算，Oswald 行高更高、大字級內部留白更多 ⇒ 四個字母的字腳壓在面板底邊或掉出去。
**user 的兩張與我自己拍的兩張都看得到，我看自己那張時只核對「這一輪改的項目」（名字疊、人數撞小標），沒把面板當一個
物件看，寫了「乾淨」。** 修＝`MeasureBig()` 用同字面同字距量實際字高當面板高度，不再猜。
**閘門**＝`Tools/UiCheck/panel_bounds_check.py`：面板最外 4px 環帶（挖掉圓角）不得有亮像素（字碰邊＝必穿過環帶），
且面板內必須有字（擋空面板假通過）；舊圖 FAIL（bottom 395 hits）、新圖 2560×1380／960×540 皆 PASS。
一版閘門用「亮像素外框」量，被面板外的白障子污染——面板外是牆不是字，亮度分不出來；量內緣環帶才對。
**紀律**：交付前的自查不是核對「我這一輪改了什麼」，是把每個容器當物件看「它有沒有包住裡面的東西」。

### 12.12 房碼面板的光學置中（user：「這裡的間距是怎麼設計出來的？」→「講白話」→「全部檢查清楚再給我看」）
數字在網格上，眼睛沒看：房碼字往下沉，因為面板高度用**行框**算，行框含字型上伸的看不見留白。
三輪才對：①用 PIL 量 Oswald 的上伸／大寫高推行框頂→墨跡頂＝字掉出底（Slate 的行框上緣留白比 PIL 大）；
②改用 Slate 自己的 `GetBaseline`，但大寫高用名目字級 ×0.81＝字貼到上一列（**Slate 字級是 pt，實際像素 ×96/72**，
實量大寫 87px 對名目 82×0.81＝66）；③em 像素數從 Slate 量到的行框反推（Oswald 行框 1.482em），大寫高 0.81em、
盒高再加 0.04em 給數字過衝與陰影、列與房碼之間用 Gap 4（小標行框底下本來有 ~9px 下伸留白）。
**實量**（`panel_bounds_check` 之外另量列的墨跡分群）：2560×1380 上空氣 14／下空氣 16；960×540 為 7／7。
**鐵則：字在盒子裡的位置不准用模型猜，要量渲染結果；字級與像素之間有 pt→px 的 1.333，行框與墨跡之間有上伸留白。**

### 12.13 大廳三修（user：「你覺得這是好的排版設計嗎？」→ 我答不是 → 「改完我再看」）
四件不需裁決的事：①上緣祈使句反映狀態——人不夠寫「WAITING FOR N MORE」（新鍵 `ImpLobbyNeed`，13 語；
**佔位符是 `{0}` 不是 `%s`**，一版印出「%S」），夠了才「START WHEN EVERYONE IS IN」；底部狀態句刪除
②空位不畫灰方塊，只畫已加入的臉（容量由面板的 N / 6 講）③大廳期間鍵帽欄掛在右欄（現金／房碼面板）底下 32
（`RightColumnBottomY` 每幀由 DrawTopBar 算），右欄＝現金→面板→鍵帽一條節奏，其他相位維持置中
④大廳不畫準星。連帶：房碼字元再剔 J／Q（Oswald 的下伸尾巴戳出面板底，閘門抓到 bottom 10 hits）＝18 字母。
驗證＝四人真局三張（2/6 主機、4/6 主機、4/6 客戶端 540p）閘門全 PASS；截圖 `Saved/UiMock/lobby4_v9_*`。
**仍待裁決**：取景（固定機位 vs 第一人稱名冊）。

**12.13 已由 user 打回還原**（「眼球像是被挖出來一樣，給我改回上一版」）：四項全部退回 12.12 的狀態
（空位灰方塊、底部狀態句、鍵帽垂直置中、準星、上緣兩句祈使句），`ImpLobbyNeed` 鍵刪除；
只保留房碼字元剔 J／Q（字戳出面板底的 bug，不動版面）。還原腳本 `Saved/UiMock/ref/revert_lobby2.py`。
教訓：那四項是我認為的改善，user 看了認為更差；**「不需裁決」的判斷本身就是裁決**，改了版面就該先給他看再重拍。

*十二、節建立 2026-09-06。與實作對賬日期：2026-09-06（四批全部＋12.8～12.13 還原）。*

## 十三、2026-09-07：UI/UX 大改（user 全權委託：「直接完成至大廠會端出的樣子後才結束」；BUILT-自驗、待 viewport）

**起點**：user 問「現在的 UIUX 是不是大廠會搬出的設計」。我先逐張看完 30 張局內＋6 張選單＋四人真局截圖
（此前 ESC／墨杯盤／睜眼／場間拍不到＝五輪 pipeline 才補齊；病根＝純本地旗標設在 server 端複本、
截圖視窗隨焦點在 server／client 間跳 ⇒ `local_pawns()` 兩邊都設），結論一句：**現在的 UI 是一套排版有紀律的
「相位標籤系統」，每個畫面都是同一組四件套（祈使句／鍵帽／現金／相位名），而大廠的 HUD 從「玩家此刻在下什麼
決定、賭什麼」往回推。** 12 張截圖裡最大的字有 6 張是倒數，而那 6 個倒數玩家都不能干預。

### 13.1 定案（本節取代 §11.2 的顏色描述；尺度、字體、分區照舊）

- **無主色**（user：「UI 一定需要一個主色嗎？」→ 拿掉）：白／黑／白 60%；紅只給 OUT COLD、WRONG（語義色）。
  理由＝這是一個關於「顏色落在皮膚上」的遊戲，HUD 再帶一個主色就是跟墨搶；清酒金與皮膚、木頭同暖，
  G 鍵帽在膚色上那塊金幾乎讀不出來。主色的兩份工作改由別的通道做：
  「現在能按」＝形狀與明度（一次性動作＝**實心白底黑字**；一般可按＝黑 70% 底白字；不可按＝白 35% 空心）；
  「剛剛什麼變了」＝動態（滲入、印章落下、現金跳字）。`Accent／AccentText` 保留為白、`OnAccent` 黑，呼叫點不改。
- **墨＝唯一的簽名材質**（`Tools/AssetPrep/ink_ui_textures.py` → `/Game/UI/Ink/T_UI_Ink{Splat,Brush,Edge,Dot}`）：
  印章墨漬（CORRECT／WRONG／OUT COLD 的地）、一筆刷痕（嫌疑人底線、巡禮框四邊、選單分隔線、巡禮墨線計時）、
  滲墨邊（判決／結局暗帶的上下緣）、軟墨點（怎麼玩的項目符號）。其他一切中性讓位——和紙、朱印、明朝體是
  日本風貼紙；墨不是風格，是機制。
- **每個相位一個問題、一個視覺答案；賭注永遠在決定點上**：搖夢價格貼在動詞後（`SHAKE HIS DREAM  $500`）、
  指認 ENTER 前一行「wrong: this ink stays on you · cup n / 3」、判決帶裡「the ink stays · cup n / 3」。
- **玩家不能干預的時鐘不准是畫面最大的字**：倒數數字全部退役；只有巡禮在祈使句下畫一條刷痕（整段進度）。
- **96 級字永不落在畫面中央的人身上**：相位切換的中央 96 橫幅刪除；判決／結局改**下三分之一**的滲墨暗帶
  （印章 → 臉 64＋名字 → 後果一行）。
- **準星只在有東西可瞄的相位**：站著作畫／鎖定／結局的作畫者／指認的受害者。
- **臉三個尺寸**：24 頂欄／64 列表／96 認人的時刻（指認候選、判決）。
- **祈使句 Oswald Medium**；Bold 只留 96 印章與標誌。
- **動態**（`NiUi::InkInS/InkOutS/InkRisePx/StampS`）：祈使句滲入＝alpha 0→1＋上浮 6px；印章落下＝墨漬先到、
  字晚一幀、位移 4px 收到 0；現金改變 1.6s 內在數字下浮 +/− 差值（進帳白、扣款紅）。

### 13.2 逐面（都有截圖：`Saved/Screenshots/WindowsEditor/uiall_*.png`、`menu2_*.png`、`lobby4_*.png`）

| 面 | 改了什麼 |
|---|---|
| ESC 選單 | 重建：左欄 MENU 48＋刷痕線＋ RESUME（實心白）／HOW TO PLAY／SETTINGS／LEAVE THE ROOM；右欄房碼（有的話）＋玩家列（臉 40、房主皇冠、KICK）；怎麼玩＝五條墨點項目（`HowTo1~5`，WrapTok 換行）；設定另一頁；底部 ESC 鍵帽。此前是全站唯一還停在小寫 Noto 的畫面。 |
| 儀式（轉瓶／入座／下一回合） | 只剩上緣祈使句；倒數與中央 96 字刪除；無準星。 |
| 作畫（站著） | 相位開始那一幀視線落到受害者身上（`UpdateCinematicCamera` 相位切換偵測）；G 鍵帽白實心＋價格。 |
| 作畫（鎖定） | 右上 96px 身體剪影＋鎖點（黑心白環；背面＝空環）＝你在身體的哪裡；chip 前加筆剪影；動詞 INK／DIP／SWITCH PEN；麥克筆貼圖 NeverStream（首兩秒整支糊＝貼圖串流）。 |
| 搖夢 | 攻擊者：現金跳字 −500，畫布中央那行字刪除；被拒才在底部一行紅字。受害者：上緣 32 級一行＋漸層，不再是橫跨線上的 96 紅字。 |
| 醉夢 | 上下暗帶拆除；% 改 18 級底部；姿勢火柴人改白 70% 線稿。 |
| 睜眼未現身 | 右緣 SHIFT FEIGN SLEEP (HOLD)、底部 WASD GET UP · ENDS THE ROUND（白實心）；現金與規則塊隱藏；底部句子刪除。 |
| 巡禮 | 「PIECE n / m」＋刷痕進度線；作品的**手刷框**（筆劃點 UV→世界→投影的包圍盒、最小 120px、四邊刷痕角不閉合、鏡頭 blend 後滲入）；無準星。 |
| 指認（受害者） | 候選臉 96＋刷痕底線；「PIECE n / m」＋賭注句，底部漸層鋪地；E／Q 循環作品（1-9 退役：作品在世界裡沒有編號）；ENTER 白實心；當前作品同款手刷框。 |
| 判決 | 下三分之一暗帶：印章 CORRECT!（白）／WRONG（紅）→ 臉 64＋名字 → TAKES THE SEAT／the ink stays · cup n / 3；被指認的作品手刷框。 |
| 結局 | 輸家不再是深藍空畫面：鏡頭繞自己的身體 9°/s（`ViewOrbitSelf`）、OUT COLD 印章、現金 −N 飛走；陳舊的 TRACE 鍵帽拆除；PostGame 不再冒用結局橫幅。 |
| 場間 | 每幅碳黑刺青掛「LASER · $2,000 ／ N LEFT」（第一幅＝按 L 會打的那幅，實框；其餘 55%）；永久刺青標 PERMANENT。 |
| 大廳 | **二版（09-07 → 09-08 三修）**：**局內視角＝第一人稱自由走動**（user 打回固定機位＋列隊：Among Us／Lethal Company／Phasmophobia 的大廳都是局內視角自由走動，Liar's Bar 的固定舞台是少數；我拿錯參照）。出生在席位環、新來的人面向圈心（`ClientLobbyFace` Client RPC＝控制旋轉是客戶端權威）。HUD＝一句狀態（上緣「WAITING FOR PLAYERS · n / m」／「READY · n / m」，客戶端只看人數）＋**左下房碼無面板**（小標 13／房碼 64 實量置中／`C COPY` 鍵帽→剪貼簿；user：「有框的房碼卡明顯不是同一套 UI」⇒ 拿掉框，改與上緣同語言＝字帶陰影＋底部一層對稱的無邊界漸層，房碼與臉列站在同一片地上）＋一列人（底部中央、臉 64、房主皇冠）＋兩顆鍵（右緣 ENTER START／ESC MENU）。右上留空；踢人住 ESC 選單；房碼字元表真的剔掉 J／Q。`ViewLobby`／`PlaceLobbyArc` 留在原始碼但未接線（固定機位版的殘骸，下次清）。真局截圖 `lobby4_host00000.png`／`Saved_P2/.../lobby4_client_P200000.png`。 |
| 個人檔案 | 力士停舞、每 8 秒轉一圈（`ANiceInkMenuStage` 讀 `IsProfileShowcase`）；「$ —」佔位符不畫。 |
| 語音 | `UNiceInkGameInstance::IsPlayerTalking`（EOS RTC `IsPlayerTalking`；本人＝登入名、他人＝NetId「EAS\|PUID」後半）→ 臉章外圈白環；沉睡者端本來不畫臉 ⇒ 無方位護欄自動成立。LAN／PIE 恆無，**未在真 EOS 局驗證**。 |

### 13.3 儀器與坑

- robo 鉤子：`DebugRoboInkTray(bool)`（與 `bDebugPaintHeld` 同款輸入源）、`DebugRoboSystemMenuPage(int)`、
  `DebugRoboCarbonize()`（結算前把第一幅轉碳黑＝場間雷射標籤的前提）。`robo_ui_shots_all.py` 二版＝31 張。
- **HighResShot 下一幀才抓**：拍完再改狀態要隔一站（一版 ESC root 頁被下一個呼叫蓋掉）。
- **純本地旗標要設在截圖視窗那一個世界的本地 pawn 上**（`local_pawns()` 兩個世界都設）——一版設在 server 複本上，
  五輪拍不到 ESC 與墨杯盤，探針還讀到 True（讀的是 server 的那份）。
- PIE 的相位字串是 `POST_GAME`（一版比 `POSTGAME` 永遠比不中 ⇒ 場間從來沒拍到過）。
- 圖示重匯入要在渲染之後再跑一次（一版順序反了，14 顆新圖示沒進庫、現金圖示消失）。
- `C4458`：成員函式參數不准叫 `Owner`（AActor 有同名成員）。
- **堆疊文字的間距要量墨跡，不能量行框**（2026-09-08 血價，user：「這行字和下面的房號之間的間距為何是這樣？」）：
  大廳小標→房碼我寫 `Y = 房碼墨跡頂 − Gap(10) − 行框高`，行框底下還有一截下伸空白（實測 6px）夾在中間
  ⇒ 我寫 10，眼睛看到 16。修＝新原語 `TokBaselineFromTop`（行框頂→基線＝全大寫的墨跡底，只用 Slate 的
  `GetBaseline`，不猜字型常數）＋ `TokCapTopOffset`（DrawTok 版的 `BigCapTopOffset`）。
  **這是 09-06「光學置中三輪」的同型第二次——凡是把兩行字堆起來，寫的必須是墨跡到墨跡的距離。**
- **比例錯了要撐開遠的那邊，不是壓扁近的那邊**（同日二修，user：「直接幾乎沒有間隔？？？」）：
  一版我把小標壓到房碼上方 4px 去湊 1:4，被打回。**16px 當初其實是合理的值**（kicker 的標準＝約等於
  小標自己的字高），錯的是下面那段只有 24 ⇒ 三行讀成等距。現制＝小標→房碼 12（3U）、房碼→鍵帽 32（8U），
  實測 14 : 44＝1 : 3.1，兩個間距各自都舒服。**分組不是把組內壓到最小，是讓組外明顯更遠。**
- **PIE 開局 OOM 的真兇是存檔不是程式**：robo 造出的碳黑刺青被 PostGame 落盤，下一輪開局 `RestoreWork→RebuildRenderTargets`
  把行程推到 19GB。定罪＝stash 全部 Source 重編 HEAD 照樣死、搬走 .sav 立刻活。pipeline 現在開跑前清 robo 存檔槽；
  這是遊戲 bug（帳＝SHIP_PLAN 追記118）。同批保險：pipeline 鎖檔＋harness 90 秒停滯即中止；等待改短輪詢。
- 作品框的背面點過濾（法線對鏡頭）試過一版：框跑到肩膀、把看得見的線排除——法線方向不可信，已拿掉。
- 鎖定畫面偶發整面黑（09-06 已記帳的暫時性現象，本輪再見一次、下一輪正常）。

### 13.4 待 user 裁決（本輪刻意未做）

偷瞄鍵（SPEC 仍寫 Shift、實作 07-18 退役）／指認時受害者的考慮公開給全房／說話者指示的判讀排序影響／
大廳取景／儀式相位的曝光壓低（fullbright 是 SPEC 定案）。

*十三、節建立 2026-09-07。與實作對賬日期：2026-09-07。*

## 十四、間距的準則與來源（2026-09-08；user：「你的排版準則到底是從哪裡來的？我怎麼覺得有點失控」→「去找」）

**先承認**：09-07~08 我為大廳房碼的間距連續給了 4、12、32 幾個數字，每個都配一套聽起來像規則的說法，
**而那些數字沒有來源，是我當場編的**。專案裡原本有來源的只有尺度表本身（字級 13/18/96、鍵帽 32、
列距 64、邊距 24＝09-04 量 Meccha 1080p 實機）。本節補上缺的那一半＝**業界的準則**。

### 14.1 業界實際有的三條（可查證）

1. **接近＝關聯（proximity）。** IBM Carbon 明文：「設計中彼此靠近的元素會被看成有意義地相關；
   元素之間加入越多空間，它們被感知到的關係就越弱。」Atlassian 同義：「彼此靠近的元素會被假定為相關」，
   並要求「把相關的物件放得更近、較不相關的放得更遠」。**這是我們這個案子唯一直接適用的規則。**
2. **用空間分組，不要用框線。** Carbon：「空間也可以用來標示一組相關的資訊。這讓頁面產生內容分區，
   而不必動用線條或其他圖形元素當分隔。」⇒ 房碼卡拿掉框、改用空間分組是**規則要求的**，不是喜好。
3. **從固定的級距表取值，不要用任意數。** Carbon 的 spacing token＝2／4／8／12／16／24／32／40／48／64／80／96／160；
   Atlassian＝8px 基準的 token（0～80）。上位原則是 Bringhurst：「排版裡的空間如同音樂裡的時間。
   它可以無限細分，但少數幾個成比例的間隔，遠比無限多的任意數量有用。」

### 14.2 業界**沒有**的兩件事（同樣重要）

- **沒有數值比例規則。** Carbon 與 Atlassian 都只給 token 表與 proximity，**都沒有規定組內比組外要幾比幾**。
  常被引用的「組內 8、組外 24（1:3）」是設計系統文章裡的示範（表單標籤 8、欄位之間 24），不是規範。
- **間距不綁字級。** Atlassian 明文：「這套間距系統與字體尺寸無關。」
  ⇒ **我先前講的「kicker 約等於小標自己的字高」「0.3 倍字高」是我編的，業界沒有這條。**

### 14.3 由此導出的本專案規則（唯一合法的推導路徑）

因為業界只給「級距表＋接近原則」，正確做法就是：**從級距表挑兩級，讓組內明顯小於組外，然後用眼睛驗收**。

- **組內（標籤 → 它的值）＝`NiUi::GapM`(8)**。8 是 Carbon 的 `$spacing-03`，也是我們自己的 token。
- **組外（值 → 動作／下一段）＝`NiUi::Margin`(24)**。24 是 Carbon 的 `$spacing-06`。
- 比 **1 : 3**（與設計系統文章的表單示範同值）。實測大廳：**11 : 35 像素＝1 : 3.2**（2560×1380）。
- **一律量墨跡到墨跡**（`TokBaselineFromTop`／`TokCapTopOffset`／`BigCapTopOffset`），不准用行框高推位置。

### 14.4 旁證（不是來源，只是 sanity check）

同一個「小標＋大字」堆疊在兩款參照的 1080p 實機上的實測值，用來確認 8 沒有離譜：

| 遊戲 | 元件 | 大字墨跡 | 小字墨跡 | 中間空氣 | 空氣 ÷ 小字 |
|---|---|---|---|---|---|
| Meccha | 上緣沙漏＋大數字＋字幕（`shot_03/04/06`） | 29～35 | 19 | 5／8／8 | 0.39 |
| Among Us | 大廳 `Code` ＋房號（`shot_05`） | 30 | 31 | 19 | 0.61 |
| **本專案（現行）** | 大廳小標＋房碼 | 88 | 18 | **11** | **0.61** |

兩款的大小比是 1.7:1 與 1:1，我們是 4.5:1——**手上沒有參照做過我們這個比例**，所以這張表只能當旁證。

### 14.5 記帳

- 09-07~08 的 4／12／32 全部作廢（不在級距表上、也沒有來源），現制 8／24。
- 業界沒有「幾比幾」的硬規則 ⇒ **8 與 12 之間的取捨只能由 user 的眼睛裁決**，這與專案鐵律
  「使用者的 viewport 是唯一驗收閘門」一致。若判定 8 太緊，改的是值、不是再編一套新說法。

*十四、節建立 2026-09-08。*

## 十五、2026-09-08：局內 UI 全面改用 Slate（user 定案「把遊戲中所有畫面都改成 Slate 架構」）

### 15.1 為什麼搬（這是這次改動唯一的理由）

起因是 user 指著大廳左下角問「這行字和下面的房號之間的間距為何是這樣？有符合排版的原則嗎」。
查下去發現**程式裡寫的數字不等於畫面上的距離**：

| 我以為 | 畫面實際 | 原因 |
|---|---|---|
| 間距 10px | 16px | 拿**行框高**去推位置，行框底下還有一截下伸空白（實測 6px） |
| 字級 64 | 墨跡 88px | Slate 的字級是 **pt**（×96/72），再乘 UiScale（×1.278） |

第二條讓所有「間距＝字高的幾分之幾」的推導全部失效——我拿一個螢幕上不存在的數字算比例。
而 `64` 本身也不在 `NiType` 的尺度表上，是舊尺度的殘留（token 檔裡當時還留著一句過期註解
「四階 14/18/28/64」）。

**決定性的證據**：同一個專案裡，主選單那 2,390 行 Slate **一個這種錯都沒有**，因為位置是引擎算的。
所有這類錯全部集中在 3,638 行手算座標的 canvas HUD。所以問題不是我不夠小心，是那一半沒有排版引擎。

### 15.2 為什麼是 Slate（其他選項都查過）

| 選項 | 結論 |
|---|---|
| **Slate** | ✅ 採用。純 C++ 零資產、自己擁有文字排版、主選單已在用、UMG 就蓋在它上面（不是死路） |
| UMG / CommonUI | 在沒有設計師的情況下＝Slate 再加一層 UObject 與 GC；CommonUI 解的是跨平台輸入路由，我們是輪詢輸入 |
| RmlUi | 排版最強，但官方第三方列表只有 UE4 整合、且會帶進第二套字體與本地化管線（跟 13 語表打架） |
| NoesisGUI | 商業（indie ≤ €100K，per project per platform），同樣是第二套文字管線 |
| Coherent Gameface | 不公開報價、需詢價、文件寫的是 UE4 預整合 |
| Yoga / Clay | **只算矩形**，文字量測回呼給我自己實作——解不到出問題的那一半 |

**注意 UMG 不是被「零資產規則」刷掉的**（`WidgetTree::ConstructWidget` ＋ 可覆寫的 `RebuildWidget`
表示 UMG 完全能純 C++ 建樹）。我一開始這樣講是錯的，查了引擎原始碼才更正。

### 15.3 分界線（不是全部塞進 widget——業界通例也是這樣切）

- **排版與文字 ⇒ Slate**（`Source/NiceInk/Private/SNiHud.cpp`）：相位橫幅、右緣操作列、底部姿勢列、
  右上現金、右下規則／比分、大廳房碼與臉列、指認的臉列、判決橫幅、醉夢的字、墨杯 chip、
  ESC 選單、開發者遙測、世界錨定的雷射標籤。
- **幾何與著色 ⇒ canvas**：漸層地（scrim）、墨帶、描圖盤、針尖圓環與暈、墨杯盤、兇手轉盤、
  可畫域 veil、作品框。這些不是排版問題，搬過去只是換一塊畫布。
- **留在 canvas 且有理由的三個文字**：墨杯盤的欄標、兇手轉盤的度數與提示、沉睡姿勢圖的標籤。
  它們是**單行、位置由所屬幾何定義**（環心、半徑、欄位）；把單行字抽出去會把一個物件拆成兩個系統。
  判準：**有內部堆疊或盒子的 ⇒ 搬；由幾何算出單點位置的單行 ⇒ 留。**
- **封存路徑不搬**（噴射、拳腳、迷宮的字）：那些元件永不啟動，搬死碼是浪費。

### 15.4 唯一真正重要的新原語

```cpp
NiSlate::InkGapPadding(Above, Below, InkGap, bBelowIsDisplay)
```

把「**墨跡到墨跡**」的距離換算成 Slate 的 slot padding。行框上下的空氣以前在每個呼叫點各自處理
（然後忘記），現在只有這裡處理一次。**第一版我在裡面鉗了 `Max(0,…)`，而行框天生的空氣（51px）
已經大於目標間距（21）⇒ 鉗位把設計悄悄改成 0.485 cap（規格 0.20）。padding 必須允許負值。**

### 15.5 施工中抓到的自己的錯（都被量測或截圖抓到）

1. **鉗位吃掉設計**（見上）：規格 0.20 cap、實測 0.485。
2. **Slate 的文字預設沒有陰影**，而全站規則是每一行字都有（六款參照無例外）——補 `ShadowOffset`。
3. **`MaxDesiredWidth` 不會裁切**：名字以自然寬度置中繪製、兩端都被切掉，讀成「e_desktop-54」。
   省略號要有真的邊界 ⇒ `WidthOverride` ＋ `ClipToBounds` ＋ `OverflowPolicy::Ellipsis`。
4. **上緣重疊**：canvas 的 `DrawTopBar` 還在畫同一句 ⇒「READY · 4 // 6」。**搬一區就要當場把
   canvas 那一份刪掉**，不能留著「之後再清」。
5. **`SBoxPanel` 是抽象的**（沒有 `FArguments`）——要拿 `SVerticalBox`／`SHorizontalBox`。
6. **用 `return;` 讓 canvas 函式早退會留下死碼**，UE 把 C4702 當錯誤——要整段刪。
7. **驗證工具對受測物是瞎的**（這一條最貴）：局內全相位截圖管線 `robo_ui_shots_all.py` 用的是
   **`HighResShot`，而它不含 Slate UI**——搬完之後拍出來的每一張都沒有 HUD，畫面本身卻是好的。
   同一個 repo 裡 `NiShot` 的註解早就寫著「Shot 必走 PC->ConsoleCommand」，而大廳那條線
   （`lobby4_test.sh`）用的正是 `NiShot`，所以它一路都拍得到。**換了受測物就要問：現在這支尺
   量得到它嗎？** 已改成 `execute_console_command(world, "Shot showui …", pc)`。
8. **我自己寫的 `OnPaint` 內縮框一個都沒畫出來**（同一個症狀犯兩次）：墨杯的 2×2 棋盤與滑鼠圖的
   暗底，都是用 `MakeBox(..., G.ToPaintGeometry(尺寸, FSlateLayoutTransform(位移)), ...)` 畫的，
   實測**整塊是純白**（墨杯 255,255,255、滑鼠圖 22×35 全白）。而同一批 brush 型別在
   ESC 選單的鍵帽與按鈕（走 widget、有實填）完全正確。
   **結論不是去查那個重載怎麼了，是不要在這裡自繪**——兩者都改成用 widget 組（`SBorder` 內縮做框、
   `SGridPanel` 做棋盤、`SImage` 的 `ColorAndOpacity` 上色），墨杯實測 RGB (112,53,147)＝`#703593`。
   **這正是這次搬遷的理由本身：我手算的東西會錯，引擎排的不會。**
9. **臉像的 fallback 要裁切**：頭像亭的肖像是裁好的頭形，亭缺席時退回**名冊的整張臉貼圖**——
   不套 UV 就是把整張圖縮進 96px 框裡＝一顆很小的頭。canvas 端本來就有這組 FaceUV
   （(0.30,0.22)+(0.40,0.40)），搬過去時漏了 ⇒ 新增 `NiSlate::FaceBrush(Tex, Size, bCrop)`。
10. **「兩個回答必須一致」的東西不該是兩個函式**（user 指出底部頭像變成一顆小頭浮在空白裡）：
    臉像的「用哪張圖」與「要不要 UV 裁切」我補成了 `GetFacePortrait()` 與 `FacePortraitNeedsCrop()`
    兩支。**頭像亭還在烘焙的那個時間窗裡它們會不同意**——解析器一路退回「名冊的整張臉貼圖」，
    判定式卻只看到「這個席位有臉資料」就回答「不用裁」⇒ 整張臉縮進 64px 的框裡。
    修法不是把判定式修對，是**合成一支 `GetFaceSource(PS, bOutNeedsCrop)`**：旗標跟著實際挑中的
    那一張走，構造上不可能不同意。
    連帶的驗證教訓：**我的四人截圖一路都是「亭已就緒」那條路，永遠碰不到出問題的分支**——
    要驗 fallback 就得挑一個必然走 fallback 的環境（robo PIE 的亭是 `WorldType==Game` 閘住的）。
11. **我把自己寫在同一份文件裡的頭號風險犯了一次**（user 實拍「房主剛建房時頭貼長這樣」）：
    `SNiFace` 只在 **Construct 解析一次**「臉的來源」，而大廳那一列只在名字／房主身分改變時重建
    ⇒ **房主獨自開房時簽章永遠不變 ⇒ 墊檔永遠不會換成真的頭像**（實測 30／50／75 秒都還是墊檔，
    不是慢、是不會）。canvas 版每幀重解析，所以亭一好就換上去。
    修＝`Tick` 每幀重問來源，**只有來源換了才重建 widget**（每幀成本＝一次指標比較）；
    實測 5s 就是正確的頭像。**保留模式的頭號風險就是這個：漏綁不會編譯失敗，只會畫面不更新。**
12. **同批還漏了兩段 canvas 本來就有的東西**（因為我只讀了 `DrawFaceTok` 的前半就開始搬）：
    ①墊檔的**紙框＋膚色底板**——只搬了 UV 裁切，於是墊檔讀成「一顆光頭浮在空中」而不是一張卡；
    ②**說話中的白環**（EOS RTC；語音是這個遊戲的第一頻道）。
    **搬一段程式碼之前要把那一段讀完**，不是讀到看懂第一條分支就開始寫。
13. **兩態只靠透明度分，在亮底上等於沒有分**（user：「滿色和半透明的按鍵分別代表什麼」）：
    可按 242 對不可按 221、而障子牆本身 199 ⇒ **只差 21 階**。白底上疊半透明白本來就沒有對比。
    token 檔寫的是「現在能按＝**形狀與明度**（實心白鍵帽／黑底白字／35% 空心）」，我搬過來時
    只做了透明度、把「形狀」那一半掉了。現制＝可按實心白帽＋黑字（238）／不可按黑帽＋淡白字（106），
    明暗整個反過來，任何底上都讀得出。**狀態要用形狀講，不要只用 alpha 講。**

### 15.6 保留模式的兩個新風險（canvas 沒有的）

- **漏綁 attribute 不會編譯失敗，只會畫面不更新。** 所有會變的文字一律走繫結，不寫死。
- **吃輸入。** 根節點是 `SelfHitTestInvisible`，每一層都是 `HitTestInvisible`；
  **唯一的例外是 ESC 選單**（開啟時角色端已打開游標並切 GameAndUI，Slate 按鈕天生收得到點擊，
  不必再自己拿 `GetMousePosition` 跟矩形比對、也不必自己處理 AR 鏡像的翻面）。
- 效能要重量：HUD 成本會沿著針的離散步進洩漏進 `cruise tipSpd` 那條手感契約（前科兩次）。

### 15.7 滑鼠圖示改用 Lucide 本體（2026-09-08；user：「現在的滑鼠圖案不好看，去找專業的圖案」）

> **⚠ 已於 2026-09-09 被 §15.9 取代**（改用 Kenney 線稿；`lucide_mouse_icons.py` 已刪）。本節保留的價值在那條**被推翻的理由**：「換家會打架」的前提是右緣還有 Lucide 動詞圖示，而那批圖示 09-09 全數移除 ⇒ 前提消失、結論跟著失效。**寫理由的時候要寫出它依賴的前提，否則沒有人知道它什麼時候到期。**

**沒有換一套 prompt 素材包**（Kenney／Xelu 那類）：09-06 已經因為「線條不同家＋亮起的鍵是紅色
＝第二個強調色」把 Kenney 的剪影退役過一次，而右緣**同一句話裡就有 Lucide 的動詞圖示**——
換一家的線寬與圓角進來，兩個圖示會互相打架。那條理由今天仍然成立。

**也沒有繼續自己畫整顆**：自繪的膠囊沒有 Lucide 的比例與圓角語言，就是被指「不好看」的那個。

**現制＝骨架用 Lucide 官方 `mouse`（專業繪製、ISC、與其餘 47 個圖示同一家），我們只補它缺的
那一件事：哪一顆鍵亮起。** 分界線用同一個 stroke 寬度；高亮填在外框的**內部遮罩**裡
（flood fill 求內部）⇒ 填色在構造上不可能溢出線外。烘焙＝`Tools/AssetPrep/lucide_mouse_icons.py`
（一輪 3 秒、不用開引擎，`--preview` 附同家對照），產出 `T_Ico_mouse_{left,right,scroll}`。

**烘的過程犯的兩個錯，都是「不量就寫」：**
1. **掃中央那一豎找滾輪時，把外框的上下弧也算進去** ⇒ 分界線畫到最底、高亮填滿整側。
   正解＝把該列切成連續段，取兩端都不碰外框的那一段。
2. **線寬用常數猜**（`int(px*0.0165)`，想成 24 格上的 2）＝768 上的 12px，而**實際渲出來是 60px**
   ——差 5 倍。於是「加粗的滾輪」比原本的線還細、整條藏在既有的線裡，看起來跟原圖一模一樣。
   正解＝`stroke_width()` 從字形本身量。
   同批第三個：加粗的滾輪被「內部遮罩」裁掉（輪子那條線本身不屬於內部）⇒ 不要裁。

### 15.8 圖庫調查與鍵帽形狀（2026-09-08；user：「有沒有符合我們整體設計的超大圖樣庫？按鍵也想要更專業的圖案」）

**調查結果（都有量、不是讀行銷文案）**

| 圖庫 | 數量 | 授權 | 構造 |
|---|---|---|---|
| **Lucide（現用）** | 1,544 | ISC | 24×24／2px |
| **Tabler** | 6,184（outline 5,193） | MIT | 24×24／2px |
| Phosphor | 9,000+（6 種字重） | MIT | 不同構造（多字重） |
| Iconoir | 1,600+ | MIT | 24×24 |

**Tabler 與 Lucide 的相容性是量出來的，不是看出來的。** 直接同字級並排時 Tabler 明顯偏細——
但那是假象：它的字形佔 em 較小，同字級渲出來整個比較小。**正規化到相同外框高**之後量
「線寬 ÷ 外框高」：

| 圖示 | Lucide | Tabler | 比值 |
|---|---|---|---|
| mouse | 0.091 | 0.101 | 111% |
| droplet | 0.105 | 0.107 | 102% |
| palette | 0.092 | 0.101 | 110% |
| keyboard | 0.112 | 0.142 | 127% |
| play | 0.098 | 0.111 | 112% |

⇒ 同家等級（多數 ±10%），可以混用**前提是按外框對齊、不是按字級對齊**。

**結論：不換。** 理由是需求面不是數量面——`ActionIcon()` 的 22 個動詞在 Lucide 全部有對應，
我們從來沒有因為「找不到圖」卡住過。而唯一真正的缺口（滑鼠分左／右／滾輪）**Tabler 也沒有**
（只有 `mouse`／`mouse-2`／`mouse-off`）⇒ 換圖庫救不了它，§15.7 的做法仍然是必要的。
Tabler 收在 `Saved/UiMock/ref/fonts/tabler-webfont.tgz` 當備援：**哪天 Lucide 真的缺一個圖，
它是唯一量測過相容的那一家。**

**鍵帽：圖片式的按鍵提示在構造上蓋不完。** Tabler 有 `square-rounded-letter-a`～`z`（26 個），
只蓋得住單一字母；我們的鍵包含 `ENTER`／`ESC`／`TAB`／`WASD`，還要跟著 13 語走。
所以鍵帽必須是「字排進一個形狀裡」，能改的是**那個形狀**——現制＝**外層側身（暗）＋內層頂面（亮）、
底邊比其他三邊厚**，讀成「從斜上方看一顆鍵」。與尺寸無關、任何字都適用、零資產。


### 15.9 圖示大減：全站只剩四種圖（2026-09-09；user：「這些 icon 都非常詞不達意」）

**user 的判讀**：「我知道他在畫什麼，但是我不知道和遊戲內要表達的東西之間有什麼關聯。
至於滑鼠左右鍵，則是整個 icon 太醜了。」

**病根不是選錯圖，是選錯品類。** Lucide 是**產品／應用**圖示庫，字彙是檔案・使用者・設定・
播放；我們的動作是搖夢・貼臉・裝睡・指認，在那套字彙裡沒有對應 ⇒ 每一顆都是二階隱喻
（`gavel`→法庭→指認、`moon`→夜→裝睡、`route`→路徑→描圖）。玩家要解兩次碼，
**而答案就寫在旁邊的動詞上，13 語都有**。§4.1 本來就寫著「動作圖示是選配」，§9 也寫過
「照搬他們的白色扁平圖示到紙墨介面上本來就會很怪」——09-06 那批違反了自己的規範。

**動手前先量「資訊是否守恆」**（腳本＝一次性，結果記在這裡）：

| 檢查 | 結果 |
|---|---|
| 每條提示都帶 glyph（＝「按哪裡」不靠圖示） | 21/21 |
| 每條的動詞 13 語全非空（＝「做什麼」不靠圖示） | 21/21 |
| 同組內沒有兩條動詞字面相同（＝圖示不是區別） | PASS |
| 反證：一圖多動詞 | `footprints`＝起身／起身・結束作畫；`play`＝開局／下一場 |

⇒ **18 個動詞圖示＋墨杯 chip 的 `pen`（常數、三支筆同一張）純刪零損失。**
而 `crown`／`wine`／`banknote` 是「圖就是內容」，不能光刪，處理見下。

**現制＝畫面上只有四種圖：**

| 用途 | 來源 | 為什麼 |
|---|---|---|
| 滑鼠 左／右／滾輪 | **Kenney Input Prompts 1.5（CC0）線稿版** | 輸入提示有整個品類專門為 16~40px 的 UI 畫。09-06／09-08 兩次「不能換家」的唯一理由是「右緣同一句話裡有 Lucide 動詞圖示會打架」——動詞圖示沒了，那條理由就沒了。選線稿是因為實心版的高亮鍵是**紅色**＝中性制不准的第二個強調色；線稿用「線 vs 實填」單色就講得完 |
| 現金 | **自家：穴あき銭** | 通用集只有西式紙鈔／硬幣。`¥` 是文字不是記號（數字旁邊會出現兩種書體），漢字在 20px 潰爛；**圓＋角穴**在 20px 結構不壞，而且只有日本的銭長這樣 |
| 罰酒（滿／空） | **自家：猪口兩態** | 同上——沒有任何 UI 圖示集有猪口。**滿／空是兩張圖（口の面積差），不是同一張換 tint**：亮度差在 40px 的三顆並排上讀不出「喝了幾杯」 |
| 房主 | **文字 `LobbyHostTag`** | 13 語已翻好（房主／部屋主／host／방장）。歐式王冠要先被認出來、再被翻譯一次 |

**自家記號為什麼可以自己畫，而滑鼠不行**：SPEC v4.0a 的「圖案來源＝現成向量圖源、
不得手雕」講的是**描圖圖案**（尺寸＝整個盤面）。到了 16~40px 的 chrome，現成向量反而不成立
——實測把 Twemoji `sake_1f376` 的猪口與 kamon2 的軍配烘成剪影，**40px 讀成一團塊、16px 讀成一塊斑**
（emoji／家紋是插畫尺度的實心向量）。這個尺寸只能為它畫。烘焙＝`Tools/AssetPrep/nice_ink_marks.py`
（線寬／外框高＝0.067，與 Kenney 線稿等重；8× 超取樣；`--preview` 出實寸對照）。

**資產＝來源資料夾的鏡像**（`ue_import_icons.py` 現在會刪來源沒有的）：此前只匯入不刪，
Content 裡累積了 44 個零引用圖示，而 `/Game/UI` 整個目錄在 cook 白名單裡＝全部進包。
本輪匯入 6 刪 61（含 10 個孤兒 `T_UI_*` 與 4 個 `T_InMouse*`）。

**順帶修掉的兩個結構問題：**
1. `FNiControlHint::Tex` 原本拿 `UTexture2D*` 當「哪一顆滑鼠鍵」的識別（指標比對）
   ＝**資產名是它的身分，刪一張圖就會靜靜壞掉** ⇒ 改成 `ENiInputGlyph` 列舉。
2. `tray_layout_check.py` 的 `c0g`／`c0h` 從 **09-08 Slate 搬家那天起就一直失敗**
   ——它們還在讀 canvas 那份 `NiceInkHUD.cpp`，而 chip 早就搬到 `SNiHud.cpp`。
   **閘門讀不到受測物時，它不是變寬鬆，是變成沒有意義。** 已改讀 Slate 那份。
   同批新增 `c1a`~`c1e`（動詞圖示不得回來／只准五個圖示名／資產鏡像／猪口兩態／
   兩處房主都是字），**五條都跑過負向測試確認會為了正確的理由失敗**。

**遺留（記帳不做）**：`shot_measure.py` 的「資源列右／上邊距」兩條以錢幣圖示為量測對象，
而中性制之後 `Amber = White` ⇒ 那個 amber 遮罩恆不成立、外面又包著 `if len(xs) > 30`
⇒ **這兩條在本輪之前就已經靜默不量**。換成銭之後同樣量不到，要改成量數字右緣。

### 15.10 身體小地圖拆除（2026-09-09；user：「移除這個，我們不需要」）

09-07 加它的理由是真的（鎖定後看到的是一整面皮膚，分不出在肚子還是大腿）。但 user
看到它的第一句話是**「畫面右上角這是什麼？」**——那就是它的判決：它是畫面上唯一
**沒有標籤、沒有邊界、也沒有被任何一行字指認過**的東西，還擠在現金正下方共用同一疊角落。

**教訓與 09-03「填色拆除」同型**：那次是我發明了一個需求（濃度回饋在視線外），這次是
需求為真但**載體從來沒有被介紹過**。一個要玩家自己猜的資訊圖，即使資訊正確，也是雜訊。
它要回來的話，前提是**有標籤或有邊界**（＝玩家不必問就知道它在講什麼）。

閘門＝`tray_layout_check.py` 的 `c1f`（`DrawBodyMap` 不得出現在任何一份 HUD 原始碼裡），
已跑負向測試：把繪製點塞回去它會為了正確的理由失敗。
