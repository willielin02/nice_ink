# 作畫相位 畫面清單（Draw HUD Inventory）

> **加一個元素之前，先讀完這張表。** 這份文件存在的唯一理由是 2026-09-02 的事故：
> `LINER` 同時出現在三個地方（針尖標籤／底部 cluster／提示行的 `Q needle`），
> 顏色出現在兩個地方。真因不是美感，是**沒有人擁有這個畫面**——我加 cluster 時
> 以為在收斂，實際上只拆了色票列，變成加第三份。
>
> 規則：**一件事只講一次，而且講在會被讀到的地方。** 新增一列時必須填滿
> 「何時消失」與「誰在讀」——填不出來的元素就不該存在。
>
> **這張表不寫數值，只寫旋鈕名**（秒數、尺寸、alpha 都在程式裡）——
> 註解裡的常數會過期，而且沒有東西在看著它（專案鐵律，血價見 SHIP_PLAN 追記80）。

## 承重前提：眼睛在哪

作畫時 FOV 36、相機凍結、畫面歸針 ⇒ **視線幾乎 100% 釘在螢幕中心（針尖）**。
這決定了每個元素的判決：

- **0°（螢幕中心）**＝落筆當下會被讀到 ⇒ 只有「墨會落在哪、什麼顏色」配得上這裡。
- **週邊（上緣／下緣）**＝只有停筆時才會被讀 ⇒ 放環境資訊與事件，不放操作狀態。
- **不會被讀的東西不該常駐**——它不是資訊，是雜訊。

## 常駐元素

| 元素 | 位置 | 出現 | 消失 | 誰在讀 | 程式 |
|---|---|---|---|---|---|
| 相位橫幅（DRAWING＋受害者臉/名/杯數） | 上緣中央 | 相位期間 | **按住 LMB 時淡到 35%** | 停筆時確認「還在畫誰、他喝幾杯」 | `DrawTopBar` |
| 現金 | 右上 | 全程 | 同上（隨橫幅淡） | 停筆時判斷買不買得起搖夢 | `DrawTopBar` |
| 落點指示（小方點，當前墨色） | **螢幕中心＝針尖** | 鎖定且可落墨 | 搆不到＝改紅 ✕ | 落筆當下：墨會落在哪、什麼顏色 | `DrawInkCrosshair` |
| 筆刷範圍圈（僅打霧） | 螢幕中心 | 鎖定＋Shader | 換筆即消失 | 落筆當下：這一下蓋多大 | `DrawInkCrosshair` |
| 行進蟻虛線＋游標十字（僅割線） | 針→游標 | 鎖定＋Liner | 換筆即消失 | 落筆當下：針還要走多遠 | `DrawInkCrosshair` |
| **墨杯 chip**（當前色×濃度＋百分比） | 下緣中央 | 鎖定 | 托盤開啟時讓開；**按住 LMB 時淡到 35%** | 停筆時確認「我現在裝的是哪一杯」 | `DrawInkChip` |
| 可畫域灰紗 | 皮膚上（世界） | 鎖定 | 出鎖 | 落筆當下：哪裡畫不到 | 角色端 `UpdateReachVeilShell` |

## 按需元素

| 元素 | 觸發 | 消失 | 誰在讀 | 程式 |
|---|---|---|---|---|
| 墨杯盤（10 色 × 3 濃度） | 按住 RMB | 放開 | 換色/換濃度的那 0.3 秒 | `DrawInkTray` |
| 搆不到提示（琥珀色） | 搆不到 >1s | 搆得到 | 卡住的人 | `DrawBottomHint` |
| 翻身表決 | 有人提案 | 表決結束 | 全體作畫者 | `DrawBottomHint` |
| 搖夢回執 | 按 G 之後 | 2s | 攻擊者本人 | `DrawHUD` |
| 系統選單 | ESC | ESC | 想離開的人 | `DrawSystemMenu` |

## 常駐操作列（形式照 Meccha 實物，2026-09-02 四修）

| 元素 | 位置 | 出現 | 消失 | 程式 |
|---|---|---|---|---|
| 鍵帽縱列（鍵帽＋2~5 字動詞） | 右緣中央 | 鎖定＝LMB/RMB/SCROLL/Q/G/WASD；站著（作畫相位）＝F/G | **按住 LMB 時淡到 30%**；隨狀態增減 | `DrawControlStrip` |

> **一次性教學卡已退役。** 我先前寫「常駐操作表＝onboarding 沒做的簽名」，看了 Meccha
> 實物才知道那句是錯的——這個品類的第一名就是常駐的。**差別不在常駐與否，在
> 「句子 vs 鍵帽＋動詞」**：鍵位要畫成鍵盤上的樣子，寫在句子裡的一個英文詞玩家不會
> 讀成「一顆可以按的鍵」。

## 已刪除（不要加回來）

| 元素 | 刪除理由 |
|---|---|
| 針尖旁 `LINER`／`SHADER`／`STENCIL` ×2 文字標籤 | 三支筆在針尖的視覺**本來就完全不同**（稿筆＝紫點＋2D 麥克筆／割線＝細針＋行進蟻／打霧＝粗針＋範圍圈）——標籤在標一件已經不會認錯的事 |
| 底部 cluster 的筆名＋`Q`＋三格直排＋`SCROLL` | 同一件事的第三份；三格直排在 23px 下棋盤讀成髒點（在托盤 60px 有效的裝置縮到 23px 就失效） |
| 常駐六項提示行 | 永久掛著的操作表＝onboarding 沒做的簽名；改成第一次入鎖時出現一次 |
| 十色票列（10 個色塊＋數字標） | 選項的可視化歸墨杯盤；常駐只需要「我手上是哪一杯」 |

## 閘門（防這一頁自己過期）

- `Tools/UiCheck/hud_loc_lint.py`：`GUARDED` 名單（作畫相位 8 個函式）裡出現會上畫面的
  裸英文字面量就 exit 1。**它在第一次跑就抓到我漏的 5 條**——包括兩個我以為已經刪掉的
  `STENCIL` 針尖標籤（我只刪了 LINER/SHADER 那一份）。負向測試：把一條裸字串塞回
  `DrawInkChip` ⇒ 為正確的理由失敗。
- `NiceInkLocText.cpp` 的 `static_assert`：鍵數與 13 語表列數不一致＝**編譯失敗**。
  舊寫法 `GTable[COUNT]` 少一列照樣編得過（補 nullptr）＝執行期空字串的無聲失敗。
  負向測試：加一個沒有對應列的鍵 ⇒ 編譯以該訊息失敗。
- `Tools/UiCheck/tray_layout_check.py` / `tray_cursor_check.py`：版面與游標增益。

## 記帳（刻意未做）

- **機器染墨色（diegetic）**：`SM_TattooMachine`／`Grip`／`Needle` 三件全部綁單一貼圖材質
  `M_TattooMachine`、槽名皆 `TattooTex`，**沒有可染色的參數**（headless 實測，
  `Saved/grip_probe.txt`）。要做＝改材質圖＋美術裁決 ⇒ 不歸程式單方面決定。
  連帶發現：`NiceInkCharacter.cpp` 依槽名上色那段（TattooCoils／TattooGrip／TattooBrass）
  在現行資產下是**死碼**（全部 `continue`），留著是非貼圖資產的退路。
- **局內 HUD 其餘相位的本地化**（巡禮／指認／醉夢／結算）：本批只做作畫相位。
  防再犯的閘門＝`Tools/UiCheck/hud_loc_lint.py`（新字串進不了表就讓檢查失敗）。
